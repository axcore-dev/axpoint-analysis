# 2026-08-04 작업 기록

## 11:15

### collect 1단계 재구성 — 진입 즉시 분류 + 순차 스텝 위저드 (사용자 인터뷰 확정안)

- **진입 판별 변경** (`app/(site)/collect/page.tsx`) — 기존 '분류 행 중 pending 아닌 것 존재'
  기준을 버리고 `GET /api/assessments/:id`의 `assessment.systems`가 비어 있지 않으면(프로그램
  선택을 마친 적 있는 재방문) 2단계 직행. 그 외에는 1단계 진입과 **동시에**
  `POST /classify`(body 없음 — pending 전체) 호출. 파일 0건이면 호출 생략, 실패해도 진행은
  막지 않는다.
- **1단계를 순차 스텝 위저드로 분리** — 상단에 도트+이름 단계 표시(랜딩 DotProgress 패턴
  축소판, ① 자료 확인 ② 사용 프로그램 ③ 사전 설문).
  - **스텝 ① 자료 확인(검증)**: 분류 진행 중에는 ClassifyProgress(분류 텍스트 로그)를 상단
    표시(3초 파일 폴링이 이 스텝에서도 동작). 분류가 끝나면 required-docs를 재조회해 필수
    서류 패널이 분류 결과가 반영된 충족/부족을 보여준다. '부족한 것만' 토글·슬롯별
    올리기·'한번에 올리기' 기존 UI 유지. 추가 업로드는 업로드 직후
    `POST /classify {fileIds: 새 파일}`로 그 건만 분류하고 폴링 재개. 패널 헤더에
    '자료 편집' 버튼 추가 — 기존 FileEditBoard(8영역 칸반 팝업)를 그대로 열어 부족·오분류를
    이 자리에서 수정(분류 진행 중 비활성, 저장 시 폴링 재개·required-docs 재조회는 기존
    분류 완료 감지로 자동). 진행 버튼 '자료가 충분해요'는 분류 완료 후 활성(워커 멈춤 대비
    기존 3분 classifyStuck 해제 재사용), 파일 0건이면 '자료 없이 진행' 노출 — 둘 다 스텝 ②로.
  - **스텝 ② 사용 중인 프로그램**: 기존 프로그램 선택 블록(배타 토글·기타 직접 입력) 단독
    화면. '다음' → `PATCH {systems}` 저장 후 스텝 ③ (저장 실패는 진행을 막지 않음).
  - **스텝 ③ 사전 설문**: 기존 primary 인라인 카드 단독 화면. '다음' → `PUT surveys` 저장 후
    2단계(자료 분류)로 전환.
- **2단계(자료 분류)는 변경 없음** — 그리드·자료 편집 팝업·공개데이터·워크플로우·보완 설문
  배너 그대로. 2단계 진입 시 분류가 이미 끝나 있으므로 폴링은 첫 조회에서 스스로 멈추고
  '진단 결과 보기' 게이트도 기존 로직 그대로 맞물린다.
- 기존 문구 보존('자료가 충분해요'/'자료 없이 진행'/'한번에 올리기'/'자료 편집' 등), 새 문구는
  스텝 이름('사용 프로그램')과 '다음' 버튼뿐.

검증: `npx tsc --noEmit` 통과. eslint 기존 4건(react-hooks/set-state-in-effect)은 HEAD와 동일
(신규 위반 없음). 커밋 금지 지시로 커밋하지 않음.

**커밋 문구** (검수 후 사용):
```
feat: collect 1단계 재구성 — 진입 즉시 분류 시작 + 자료 확인·프로그램·사전 설문 순차 스텝

- 재방문 판별을 assessment.systems 기준으로 변경, 그 외 진입과 동시에 classify(pending 전체)
- 스텝 ① 자료 확인: 분류 로그 → 분류 결과 반영 필수 서류 검증, 추가 업로드는 fileIds 부분 분류
- 스텝 ① 패널에 '자료 편집'(FileEditBoard) 진입점 추가 — 분류 중 비활성, 저장 시 폴링 재개
- 스텝 ②③: 프로그램 PATCH·사전 설문 PUT 저장 후 전환(저장 실패는 진행 비차단), 도트 단계 표시
```

## 11:39

### 자료 정리 화면 개선 4건 — 더미 로딩 제거·스텝 ① 레이아웃·하단 버튼 행·칸반 소분류

- **업로드→자료 정리 전환 더미 로딩 제거** (`app/(site)/collect/page.tsx`) — 진입 시
  고정 4200ms `booting` 타이머 연출 삭제. RouteLoading은 실제 진입 판별
  (`GET /api/assessments/:id` + 초기 파일 조회) 대기 동안만(`stage === null`) 보이고
  끝나면 즉시 전환. RouteLoading 컴포넌트·순환 문구는 그대로(다른 화면 공용).
- **스텝 ① 자료 확인 레이아웃 정돈** —
  - 스텝 인디케이터 `mt-10→mt-8`, 제목 header `mt-6→mt-3`(세 스텝 공통)로 인디케이터-제목
    수직 리듬을 좁혀 중복감 완화(인디케이터 크기·타이포는 유지).
  - 분류 진행 중 ClassifyProgress를 테두리 카드(`border-line` + 헤더 라벨)로 감쌈 — 라벨은
    2단계 기존 문구 재사용: "AI가 자료를 분류하고 있어요 (n/m)".
  - 필수 서류 패널 스크롤 영역 `max-h-[300px] → max-h-[max(300px,60vh)]`로 화면 높이에 맞게
    확장(뷰포트가 낮으면 패널 내 자연 스크롤).
  - 1단계 컨테이너 하단 패딩 `pb-24 → pb-12` — 버튼 행이 화면 하단에 자연스럽게 앉게.
- **스텝 ① 하단 버튼 행(사용자 지정)** — 한 행에 `자료 편집`(secondary) + `다음 →`(primary,
  flex-1). '자료가 충분해요' 문구를 `다음 →`로 교체(사용자 지정). `다음 →` 활성 조건은 기존
  게이트(분류 완료 or classifyStuck) 그대로. 파일 0건이면 '자료 없이 진행' 유지 + 자료 편집
  비활성(분류 중·업로드 중에도 비활성). 필수 서류 패널 헤더의 '자료 편집' 버튼은 하단 행으로
  이동했으므로 제거(중복 진입점 정리, 2단계 그리드의 '자료 편집'은 유지).
- **자료 편집 칸반 — 소분류 단위 이동·업로드** (`components/flow/FileEditBoard.tsx`) —
  - CardState에 `docTypeId`(사용자 선택 소분류, null = '자동 분류') 추가. 이동(드롭)·새 업로드
    카드에 그 영역의 소분류 select 노출(기본 '자동 분류', 목록은 GET /doc-groups의 그룹별
    docTypes). ISO 컬럼은 기존 관리영역 select 유지 + 그 관리영역의 소분류 select 추가(관리영역
    변경 시 소분류는 '자동 분류'로 리셋 — 목록이 달라진다). 컬럼 이동 시에도 리셋.
  - 저장: 소분류를 고른 파일은 `PATCH /api/files/:id/doc-type {docTypeId}`(유형 확정,
    method=user) — 재분류 불필요라 classify 대상에서 제외. 단 팝업에서 새로 올린 파일은
    분석(디지털화 수준·본문 추출)이 없어 classify fileIds에 포함(워커 userAsserted 경로가
    유형은 유지하고 분석만 채움). '자동 분류'는 기존대로 `PATCH group` + classify.
  - '+ 파일 추가'는 기존대로 즉시 업로드 후 카드 추가 — 새 카드에 소분류 select가 바로 열려
    선택 사항으로 지정(카드 추가 직후 select 노출 방식). 팝업 상단 안내 문구 유지.
- **편집 팝업 너비(이전 요청 잔여)** (`app/globals.css`, FileEditBoard) —
  `.ax-modal--full` 폭 `calc(100vw-40px)`/max 1560px → `min(96vw, 1880px)`. 컬럼은
  `w-[200px] flex-none → min-w-[170px] flex-1`: 데스크톱에서는 컬럼이 가로를 나눠 채워
  가로 스크롤 없이 다 보이고, 좁은 뷰포트에서는 min-width로 기존 가로 스크롤 유지.

검증: `npx tsc --noEmit` 통과. eslint는 HEAD와 동일한 기존 5건(react-hooks/set-state-in-effect)
외 신규 위반 없음. 커밋 금지 지시로 커밋하지 않음.

**커밋 문구** (검수 후 사용):
```
feat: 자료 정리 개선 — 더미 로딩 제거·스텝① 레이아웃·하단 버튼 행·칸반 소분류 지정

- collect 진입 고정 4200ms 연출 제거, 실제 판별 fetch 대기 동안만 RouteLoading 표시
- 스텝① 인디케이터-제목 리듬 정돈, ClassifyProgress 카드화(기존 문구 라벨), 패널 max-h 60vh
- 하단 한 행: 자료 편집(secondary) + '다음 →'(primary, 문구 교체) — 패널 헤더 중복 진입점 제거
- 칸반 카드 이동·추가 시 소분류 select(기본 '자동 분류') — 지정 시 PATCH doc-type 확정,
  새 업로드만 분석용 classify 포함, 자동 분류는 기존 PATCH group + 재분류 유지
- 편집 팝업 폭 min(96vw,1880px), 컬럼 min-w 170px + flex-1로 데스크톱 가로 스크롤 제거
```

## 15:53

### UI 개선 4건 — 결과 통계 칩 교체·레이더 라벨 잘림·워크플로우 카드·분류 로그 아코디언

- **진단 결과 통계 칩 스펙 교체** (`app/(site)/result/page.tsx`) — 사용자 확정 구성·순서로
  재배열: ① 연 매출(기존) ② **스마트공장**(rnd 행 itemCount>0 → '수혜 기업', 아니면
  '수혜 이력 없음' — 기존 '정부 R&D 과제' 칩 대체) ③ 특허(서버 신규 `registeredCount` →
  '등록 {n}건') ④ 최근 보도(기존 '{n}건') ⑤ 고용(기존 '{n}명') ⑥ 조달 실적(신규 `amountWon`
  합산 — '{n}건 · {억 단위 소수1자리}억 원', 금액 0이면 '{n}건'만) ⑦ 인증(기존 벤처·이노비즈).
  `PublicStat` 타입에 `registeredCount?`·`amountWon?` 추가, 일회용 countChip 헬퍼는 제거.
- **레이더 차트 좌측 라벨 잘림 수정** (같은 파일 RadarChart) — viewBox를 `0 0 360 336` →
  `-40 0 440 336`으로 좌우 40px 여백 확장. 'AI활용 및 성과' 등 좌·우 끝 라벨이 컨테이너 밖으로
  나가지 않는다(SVG 비율 스케일이라 좁은 폭에서도 동일). 호버 툴팁 x 클램프도 새 경계 기준으로
  보정.
- **워크플로우 카드 내용 잘림 개선** (`components/flow/WorkflowStandard.tsx`,
  `WorkflowSection.tsx` — collect·result 양쪽) — 카드 `minWidth 260 → 300`(+`maxWidth 340`으로
  줄바꿈 유도, 높이는 기존대로 자동). 업무 Task 항목은 line-clamp-2. 산출 문서 뱃지는 공용
  `DocBadge`로 교체 — nowrap 한 줄 잘림 대신 높이 auto + 2줄 말줄임(뱃지 행 flex-wrap 기존
  유지).
- **분류 진행 로그 아코디언 보존** (`components/flow/ClassifyProgress.tsx`,
  `app/(site)/collect/page.tsx`) —
  - 상단 페이드 마스크는 항목 2개 이상일 때만 적용(1건이면 또렷하게).
  - `done` prop 추가: 분류 완료 후 사라지지 않고 우측 정렬 접힌 아코디언(토글 '분류 과정
    보기')으로 최종 로그 보존. 펼치면 overflow-y-auto로 휠 스크롤 허용, 자동 스크롤·마스크는
    진행 중에만. 스텝 ①(필수 서류 패널 위)과 2단계(파일 그리드 상단) 동일 규칙 적용.

검증: `npx tsc --noEmit` 통과. 커밋 금지 지시로 커밋하지 않음.

**커밋 문구** (검수 후 사용):
```
feat: 결과 통계 칩 스펙 교체 + 레이더 라벨 잘림 수정

- 칩 순서 확정: 연 매출·스마트공장(수혜 여부)·특허(registeredCount 등록 건)·최근 보도·고용·
  조달 실적(itemCount건 · amountWon 억 원)·인증 — PublicStat에 registeredCount?/amountWon? 추가
- RadarChart viewBox 좌우 여백 확장(-40~400)으로 5축 라벨 잘림 제거, 툴팁 클램프 보정
```
```
feat: 워크플로우 카드 잘림 개선 + 분류 로그 아코디언 보존

- 카드 minWidth 300(max 340), Task 목록 line-clamp-2, 산출 문서 뱃지 2줄 허용(DocBadge 공용화)
- ClassifyProgress: 페이드 마스크는 항목 2개 이상일 때만, 완료 후 '분류 과정 보기' 우측
  아코디언으로 보존(펼침 시 휠 스크롤 허용) — 스텝①·2단계 동일 적용
```

---

## 16:05 — 보고서 PDF 이메일 발송 + 다운로드

- `components/report/ReportDocument.tsx`: 데모(@/data/scenario) 의존 제거, 보고서 화면 실데이터(`ReportSummary`·`ReportRoi`)로 렌더하도록 재작성. 페이지 구성 ① 표지(Lv·종합 점수) ② 진단 결과 요약(스탯 4타일·업종 대비 포지션·로드맵 요약) ③ 예상 효과 산출 내역(ROI 있을 때만) — 데이터 없는 데모 섹션(종합 소견·6축·업무영역·과제 상세)은 생략, 한계 고지는 마지막 페이지 하단
- `lib/pdf.ts`: `generateReportPdf`가 저장 대신 PDF Blob 반환(다운로드+업로드 겸용), `reportFileName()` 분리 — `AXpoint_진단보고서_{기업명}.pdf`
- `app/(site)/report/page.tsx`: '보고서 받기' 제출 시 숨김 렌더한 ReportDocument 캡처 → PDF 생성 → ① 즉시 브라우저 다운로드 ② `POST /api/leads/report`(multipart)로 업로드해 이메일 첨부 발송. 생성 중 Loader 표시. 성공 문구 '이메일로 발송했어요.', 발송 실패 시 '발송에 실패했어요. 파일은 다운로드됐어요.'
- `README.md`: report/·pdf.ts 설명 갱신

커밋 문구:
- feat: 보고서 PDF 생성 실데이터 연동 — 다운로드 + 이메일 첨부 발송

## 16:12 — 문서 정리 · README 현행화 · docs/update git 추적

- `docs/결정 대기.md`: 최상단 이관 배너 추가 — 루트 docs/대기 항목.md · 사용자 대기 항목.md 참조. 살아 있는 항목(bizNo 유실 · 구글 OAuth 동의 화면 · 이용약관 문서)만 루트로 이관, 완료 항목은 생략. 파일 삭제 없음.
- `.gitignore`: `/docs` → `/docs/*` + `!/docs/update/` — 작업 기록만 git 추적. 통짜 `/docs` 무시면 하위 예외가 안 먹으므로 `/docs/*` 형태로 교체, 주석도 실제 동작과 일치시킴. `git status --untracked-files=all`로 docs/update/*.md만 untracked로 나타나는 것 확인.
- `README.md` 현행화:
  - "백엔드(예정)… docs/DB(new)/ 참고" → `axpoint-analysis-api`(Hono :3001 + Drizzle/Postgres + BullMQ/Redis + MinIO, better-auth 쿠키 세션), 설계 문서는 루트 docs/ 기준으로 교체
  - '가짜 인증' 구절 → better-auth 쿠키 세션(이메일 인증 · Google OAuth · 게스트) + `NEXT_PUBLIC_API_URL` 안내
  - `data/` 설명에 화면 미참조(정리 보류 — 루트 docs/로직.md §5) 표기. 단 rubric/meta의 DIGITAL_LEVELS와 glossary는 화면이 참조 중이라 구분해 표기
  - `lib/scoring` · `roadmap.ts` · `roi.ts`에 (미참조) 표기 — 채점·로드맵·ROI는 백엔드 산출
  - '데이터 흐름' 다이어그램을 실 API 흐름(백엔드 분류·판정·채점 → result/report 표시 전용, PDF 다운로드·이메일 발송)으로 교체
  - 협업 규칙·폴더 구조의 docs 미추적 문구에 update/ 추적 예외 반영, 6축 → 5축
- 검증: `npx tsc --noEmit` 통과(코드 영향 없음).

커밋 문구:
- chore: docs/update 작업 기록 git 추적 전환 (/docs/* + !/docs/update/)
- docs: README 현행화 — 실 백엔드 연동·better-auth·데이터 흐름·docs 추적 규칙 반영
