## 19:10 — 수정요청v6 반영

### 공통
- 헤더 우측 상단 로그인/회원가입 버튼 + 로그인 후 프로필 드롭다운(이름·이메일 / 내 정보 / 로그아웃) — `components/flow/StepBar.tsx`, 신규 `components/auth/ProfileMenu.tsx`
- 내 정보 페이지 신규 `/mypage` — 계정 정보 + 분석 기록(더미 3건). 비로그인 가드 포함

### 자료 올리기 (`app/page.tsx`)
- 검색: 입력 전 드롭다운 미노출(최근 검색 기록 형태 제거), 예상 검색어에 씨엠텍·(주)승광 추가
- 검색→로그인→기업 확인 전달 버그 개선: 검색 즉시 `companyInput` 저장, 기업 확인 카드가 검색 기업별 메타(`COMPANY_META`)를 표시
- 현장 서류: 실제 파일 업로드(input type=file, 다중 선택)로 전환, 기존 더미 12건은 별도 '데모' 버튼으로 분리. 첨부 목록은 `DiagnosisContext.attachedFiles`로 라우트 이동에도 유지

### 자료 정리 (`app/collect/page.tsx`)
- 설문 앞으로 가기: 이미 응답한 문서/문항이면 노출, 위치 우측(뒤로 가기 반대편, `CardNav.ForwardIconButton` right:14)
- 뒤로가기 버그 수정: 완료 카드에서 뒤로 가면 처음이 아니라 마지막 설문 문항으로 이동(`reopenChain`)
- 자료 분류: 8대 영역 탭 → 워크플로우 비교로 교체 (신규 `data/scenario/workflow.ts`)
  - 제조 표준 흐름(칩 행) vs 자사 분석 흐름(부서 카드 행) 비교, 순서 다른 부서 경고 표시
  - 부서 카드: 부서명 · 업무 리스트 · 업로드 문서 리스트(디지털화 수준 L1~L4 명시 유지)
  - 자사 흐름은 드래그앤드롭으로 순서 수정 가능(네이티브 DnD, 라이브러리 추가 없음)
  - 8대 영역 분류 자체는 데이터 계층(documents.ts area 태깅)에서 유지 — 과제/로드맵에서 계속 사용
  - 설계 결정(인터뷰 없이 진행): 부서 7개 체인(영업→설계→구매·자재→생산→품질→재고·출하→고객지원) + 지원 부서(경영지원), 자사 분석은 구매·설계 순서가 표준과 다른 시나리오

### 진단 결과
- Lv.1~5 정의 문서 신규: `docs/AX레벨정의.md` (단일 원본은 `data/rubric/meta.ts` LEVELS, 데모기업 57점=Lv.2)
- 가치사슬 신호 섹션 전부 제거: `app/result/page.tsx`(아코디언·재고 흐름 표·관련 상태/임포트), `components/report/ReportDocument.tsx`(⑤ 섹션 제거 후 재번호 5·6·7). `data/scenario/valueChain.ts`는 데이터 정합성 위해 보존

### 개선 과제 (`app/tasks/page.tsx`)
- 헤더 요약의 "N개 후보 과제" 수치 제거
- 담기 바(플로팅)에 2개 이상 담을 시 '모두 해제' 버튼 노출

### 로드맵
- 단계 카드 표기 `단계 N` → `STEP N` (`app/roadmap/page.tsx`, PDF `ReportDocument.tsx` 동일 적용)

### 검증
- `npx tsc --noEmit` 통과, `npm run build` 성공(/mypage 라우트 포함 12페이지). ESLint 신규 지적 없음(기존 지적 6→5건)

## 23:09 — ponytail-review (과잉 구현 점검, 미적용·목록만)

작업 디렉터리 diff(수정 9개 + 신규 3개 파일) 대상. 코드 수정 없음, 발견 사항 기록만.

- `DiagnosisContext.tsx:28` — `AttachedFileInfo.demo` 필드: 쓰기만 있고 읽는 곳 없음 → 삭제 가능
- `DiagnosisContext.tsx:35` — `uploadSimulated`: 이제 항상 `attachedFiles.length > 0`과 동일 → 필드 삭제, dirty 체크(L92)에서 직접 계산 가능
- `data/scenario/workflow.ts:16` — `areaIds`: "8대 영역 연결 고리"로 선언했지만 읽는 코드 없음 → 필드 삭제, 매핑은 주석으로 충분
- `ProfileMenu.tsx:16,38-51` — `hovered` state + `itemStyle(i)` + 마우스 핸들러 4개는 CSS `:hover` 재구현 → Tailwind `hover:` 클래스로 대체 가능
- `app/page.tsx:157-172` — `pendingFiles` ref + `uploading` effect 조합 → 핸들러 내 `setTimeout` 하나로 단순화 가능
- `app/page.tsx:61` — `DEFAULT_COMPANY_DESC`가 `COMPANY_META["데모기업"]` 중복 → 상수 1개 제거 가능

미지적: ProfileMenu 드롭다운 자체 구현(dropdown-menu 패키지 미설치 상태에서 올바른 선택), `docById` Map, `getDept`, 드래그앤드롭(이미 최소 구현).

예상 절감: 약 −45줄.

## 23:28 — ponytail-review 6건 적용

- `DiagnosisContext.tsx` — `AttachedFileInfo.demo` 필드 삭제, `uploadSimulated` 상태 필드 삭제(beforeunload dirty 체크는 `attachedFiles.length > 0`로 직접 계산)
- `app/page.tsx` — `demo` 필드 제거, `DEFAULT_COMPANY_DESC` 상수 제거(`COMPANY_META["데모기업"]` 폴백으로 대체), `pendingFiles` ref + `uploading` effect를 `startUpload()` 핸들러 내 setTimeout 하나로 통합. 판독 1초 중 파일 삭제 시 병합이 삭제 전 목록 기준인 한계는 `ponytail:` 주석으로 표기
- `data/scenario/workflow.ts` — 미사용 `areaIds` 필드·`FunctionAreaId` 임포트 삭제 (8대 영역 연계 설명은 헤더 주석 유지)
- `components/auth/ProfileMenu.tsx` — `hovered` state + `itemStyle(i)` + 마우스 핸들러 4개를 Tailwind `hover:` 클래스(`itemClass`)로 대체

### 검증
- `npx tsc --noEmit` 통과, `npm run build` 성공(12페이지). 잔존 참조 grep 0건(`uploadSimulated`/`pendingFiles`/`areaIds`/`hovered`/`DEFAULT_COMPANY_DESC`)
