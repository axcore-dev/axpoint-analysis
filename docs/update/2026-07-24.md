## 16:58 — 수정요청v7 반영

### 공통
- 헤더 좌측 AXCORE 로고(assets/axcore-color.png, 24px, 클릭 시 홈) 추가 — `StepBar.tsx`
- (주)승광 메타 확정 반영: 금형 제조업 · 광주 평동. 기업 메타 단일 원본을 신규 `data/scenario/companies.ts`(COMPANY_DIRECTORY: 상호명·별칭·사업자번호·업종·업종축약·지역)로 이동 — 랜딩 자동완성/기업 확인, 워크플로우 라벨, 회사 검색이 공유
- 전역 타이포 +2px: `globals.css` --text-* 전 단계(캡션 13→15 ~ display1 56→58), 랜딩 스코프 토큰·스텝바 라벨(15→17)도 동반 상향

### 자료 올리기
- 검색창 한글 IME 버그 수정: downshift 제어 입력이 조합 중 재렌더로 조합을 취소해 글자가 중복·깨지던 문제 → `Autocomplete.tsx`를 입력 DOM 완전 비제어(React가 value 미기록) + 필터링용 query 미러 구조로 재작성. 외부 값 복원은 포커스 없을 때만 ref로 직접 기록, 항목 선택 시 inputValue 리셋 가드(stateReducer) 추가
  - 브라우저 검증: 수정 전 합성 조합 입력 "승광"→"괭" 깨짐 재현 → 수정 후 정상 입력·하이라이트·선택 확인

### 자료정리 — 자료 분류 (워크플로우 개편)
- 섹션 풀블리드(화면 최대 너비, mx-[calc(50%-50vw)]) + `html{overflow-x:clip}`으로 스크롤바 폭 초과 방지
- "제조 표준" → "{업종} 제조 표준"(예: 금형 제조 표준) + "표준 상세" 버튼·팝업(순번+부서+업무 리스트) 신설
- "자사 분석" → "{기업 이름}" 라벨 + 간략 흐름 필 행 추가. 간략 필도 드래그앤드롭 가능, 아래 상세 카드 행과 순서 연동(dragProps 공유)
- 카드 안 '표준과 순서 달라요' 문구 → 우상단 경고색 원형 마커(호버 설명)로 교체, 간략 필도 경고 톤 표시
- 설명 문구 삭제 → 그 위치에 "표준과 순서가 다른 부서 N곳"(숫자만 경고색, 모노)
- 브라우저 검증: 승광 플로우로 필 드래그(구매·자재→설계 뒤) 시 두 행 연동·0곳 갱신 확인

### 진단 결과
- '정부 R&D 과제' 통계 칩 제거 → '스마트공장 사업 · 수혜 이력 없음' 판단 카드로 교체, 2번째 위치 배치 — `data/scenario/company.ts`

### 내 정보
- 프로필 아바타 이름 이니셜 → 기본 사람 아이콘(Icons.user 신규)
- /mypage에서 헤더 스텝바 숨김(로고·프로필만), SiteFooter 노출 경로에 /mypage* 추가
- 계정 카드에 회사명·직책·연락처 + "내 정보 수정하기" 버튼 → 신규 `/mypage/edit`
  - 비밀번호 변경 팝업: 새 비밀번호/확인 + 실시간 체크리스트 4항목(8자↑ · 영문/숫자/특수 각 1↑ · 허용 문자만 · 확인 일치), 허용 특수문자 32자 안내(₩·백슬래시 모두 허용), 충족 시에만 변경 → 완료 토스트
  - 회사 검색 팝업: 사업자등록번호로 찾기(`사업자등록번호 10자리`) / 상호명으로 찾기(`상호명으로 찾기`) 토글, COMPANY_DIRECTORY 검색 → 선택 시 회사명 반영
  - AuthContext: AuthUser에 company/title/phone + updateProfile 추가(세션 유지)
- 분석 기록: 행별 재확인 아이콘 버튼 → 팝업(기업·일시·레벨·상태 요약). 진단 완료 시 "보고서 보기"(/report), 미완료 시 안내 후 "이어서 진행하기"(첫 미완료 단계로) — 인터뷰 확정 동작
- 버그 수정: 내 정보 수정하기·헤더 로그인/회원가입이 일반 `<a>` 전체 리로드라 진행 중 진단의 beforeunload 경고에 막히고 상태가 유실되던 문제 → router.push SPA 이동으로 교체

### 검증
- `npx tsc --noEmit` 통과, `npm run build` 성공(13 라우트). ESLint 총 지적 수 기존과 동일(6건 — 전부 사전 존재하던 규칙 부류의 false-positive)
- 브라우저(localhost:3000) E2E 확인: 승광 검색→로그인→확인→데모 업로드→설문→워크플로우 드래그→진단 결과(스마트공장 칩 2번째)→마이페이지(스텝바 숨김·푸터·재확인 팝업·수정 페이지·비밀번호/회사 검색 팝업) 전 구간 정상

## 18:01 — 내 정보 수정 비밀번호 확인 게이트

- `/mypage/edit` 진입 시 계정 비밀번호 재확인 후에만 수정 폼 노출 (`VerifyPassword` 게이트, `app/mypage/edit/page.tsx`)
  - 버튼 경유뿐 아니라 URL 직접 접근도 동일하게 차단 — 게이트를 페이지 자체에 배치
  - 페이지 진입(마운트)마다 다시 요구, 취소 시 뒤로 가기
  - 가짜 인증 컨벤션 유지(AuthForm과 동일): 4자 이상 입력 시 통과, 600ms 확인 지연
- 검증: `npx tsc --noEmit`·ESLint 통과, 브라우저에서 게이트 표시 → 비밀번호 입력 → 폼 진입 확인

## 18:40 — 워크플로우 표시 조정 + 표준 상세 팝업 개편

- 워크플로우 섹션 너비·정렬 원복: 풀블리드(화면 최대 너비)·중앙 정렬 → 기존 980px 컨테이너·좌측 정렬로 복귀 (`html{overflow-x:clip}` 전역 CSS도 제거)
- 잘림 방지: 자사 상세 카드 행을 가로 스크롤 대신 줄바꿈(flex-wrap)으로 변경 — 7개 부서 카드가 모두 보임 (간략 필 2행은 원래 줄바꿈)
- 표준 상세 팝업: 번호 리스트 → 자사 상세와 같은 부서 카드 흐름(카드+화살표, wide 모달)으로 교체. 표준에는 업로드 문서가 없어 업무 리스트만 표시(`DeptCardBody showDocs=false`)
- Autocomplete 입력값 보존 가드 강화: 특정 전이 타입별 가드 → "직접 타이핑 외에는 입력값 변경 불허" 포괄 가드로 교체 (로그인 모달 오픈 시 검색 기업이 비워져 기업 확인이 폴백 메타로 뜨던 잔존 버그 수정)
- 검증: `npx tsc --noEmit` 통과, 브라우저에서 승광 플로우로 분류 화면 렌더(줄바꿈·불일치 표시)·표준 상세 카드 팝업 확인

### 커밋 문구 제안
```
수정요청v7 반영: 헤더 로고·전역 타이포 +2px·IME 버그 수정, 워크플로우 비교 개편(업종 표준·드래그 연동·표준 상세 팝업), 스마트공장 카드, 마이페이지 확장(수정 페이지·비밀번호 확인 게이트)
```

## 18:43 — 워크플로우 가로 스크롤 복귀 + 도트 범례

- 자사 상세 카드 행: 줄바꿈 → 가로 스크롤 복귀 (기존 v6 방식, 스크롤바 숨김 유지)
- 표준 상세 팝업: 카드 흐름 가로 스크롤 유지 + 스크롤바 노출(흐름이 이어짐을 표시)
- 경고 도트 범례 추가: 비교 현황 문구 앞에 동일한 경고색 도트를 붙여 "● = 표준과 순서가 다른 부서"임을 설명 (사용자 지정 문구 "표준과 순서가 다른 부서 N곳"은 그대로 유지)
- 검증: `npx tsc --noEmit` 통과, 브라우저에서 카드 행 스크롤·팝업 스크롤바·범례 렌더 확인

### 커밋 문구 제안
```
워크플로우 표시 개선: 상세 카드·표준 팝업 가로 스크롤, 순서 불일치 도트 범례 추가
```

## 18:57 — 워크플로우 가로 스크롤 화살표 버튼

- 마우스 휠로 가로 스크롤이 안 되는 문제 → 좌우 화살표 버튼 추가 (`HScroller` 공용 래퍼, `app/collect/page.tsx`)
  - 스크롤 위치에 따라 필요한 쪽만 노출(시작점에서는 오른쪽만, 끝에서는 왼쪽만), 클릭 시 카드 2장 폭(560px)씩 부드럽게 이동
  - 적용 대상: 자사 상세 카드 행(스크롤바 숨김 유지) + 표준 상세 팝업(스크롤바 노출 유지)
- 검증: `npx tsc --noEmit` 통과(ESLint 신규 지적 없음), 브라우저에서 화살표 노출·클릭 스크롤(scrollLeft 560 이동)·양끝 화살표 전환 확인

### 커밋 문구 제안
```
워크플로우 가로 스크롤 화살표 버튼 추가(카드 행·표준 상세 팝업 공용 HScroller)
```
