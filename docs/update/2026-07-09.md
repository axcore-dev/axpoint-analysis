# 2026-07-09 작업 기록

## 14:22 — Paperlogy 웹폰트 다운로드 및 디자인 시스템 미러링 시도

### 완료
- `public/fonts/`에 Paperlogy 웹폰트 9종(woff2) 전체 다운로드 완료
  - 출처: noonnu 공식 CDN (`https://fastly.jsdelivr.net/gh/projectnoonnu/2408-3@1.0/Paperlogy-*.woff2`)
  - 가중치: 1Thin(100), 2ExtraLight(200), 3Light(300), 4Regular(400), 5Medium(500), 6SemiBold(600), 7Bold(700), 8ExtraBold(800), 9Black(900)
  - 각 파일 wOF2 매직 바이트 및 크기(약 156~164KB) 검증 완료

### 미완료
- Claude Design 프로젝트(0a6dbf03-6926-4382-87af-83202a82bad3) 파일 미러링(`docs/design-system/`, 로고 PNG 2종)은 DesignSync 도구가 현재 실행 환경에서 사용 불가하여 수행하지 못함. 도구가 가능한 세션에서 재시도 필요.

## 14:51 — S0 랜딩·S1 자료 수집/정리 화면 구현

### 완료
- `app/page.tsx` 덮어쓰기 — S0 랜딩
  - 다크 히어로(Eyebrow·56px 헤드라인·서브카피 "무료" 명시) + 히어로에 겹치는 진단 시작 카드
  - a) 기업 식별값 Input(필수, 데모 시나리오 각주) b) 업로드 시뮬레이션(점선 박스 → 데모 자료 12건 첨부, 파일명 미리보기, 지원 포맷 안내) c) 시스템·관심영역 Tag 칩(update로 컨텍스트 반영) d) "AI 진단 시작" CTA(식별값 1자 이상 시 활성, REQ-F-04) → completeStep("landing") 후 /collect 이동
  - 개인정보 마스킹 고지, mist 차별점 3카드(실물 자료·가치사슬 교차·정부사업 언어 정합), 다크 진행 안내(6단계 + 약 3분)
- `app/collect/page.tsx` 신규 — S1 자료 수집·정리 (해석·점수·등급 미노출, REQ-F-01②)
  - 가드(companyInput 없으면 안내 카드 + 처음으로), 진입 시 2.5초 3단계 수집 시뮬레이션(재방문 시 생략)
  - [확인 필요] 최상단(F-COL-04): HITL 문서 원탭 확인 2건(d07·d08) + 최소 설문 12문항(10번 조건부 미노출 안내, "데모 응답으로 모두 채우기" ghost, 전 문항 응답 시 surveyDone)
  - 공개 데이터 6종 카드(상태 Badge·건수 모노·items·note·partial 재수집 시뮬레이션, "기업 정보 기준: DART")
  - 업로드 자료 12건 카드(3축 태그·신뢰도 모노%·마스킹 배지·티저·판독 상세 토글(F-COL-05)·태그 수정 인라인 select + 수정 이력 안내)
  - 8영역 커버리지 단일 시각화(F-COL-06: 수평 바·자료 부족 라벨·missingHint·추가 업로드 안내)
  - CTA "진단 결과 보기"(문서 확인 2건 + 설문 완료 시 활성) → completeStep("collect") 후 /result 이동
- `npx tsc --noEmit` 통과 (오류 0)

### 비고
- 공유 기반 파일(lib/, data/, components/ui·flow, app/layout.tsx, app/globals.css) 수정 없음. 신규 패키지 설치 없음.
- /result 라우트는 아직 미구현 — S1 CTA 이동 대상 화면은 후속 작업 필요.

## 14:52 — S3 개선 과제·S4 로드맵 화면 구현

### 완료
- `app/tasks/page.tsx` 신규 — S3 개선 과제(통합)
  - page 기본 export는 `<Suspense>` 래퍼, 내부 `TasksContent`에서 useSearchParams (Next 16 정적 프리렌더 요구)
  - 가드: completedSteps에 "result" 없으면 안내 + 진단 결과 링크만
  - `?area=<areaId>` 진입 시 해당 영역 필터 초기 활성, 영역 필터 칩(Tag) = 전체 + FUNCTION_AREAS 8개(REQ-F-02)
  - 기본 노출 recommendedTasks, "다른 개선 과제 더보기 (N건)" secondary 버튼 → moreTasks 펼침(F-TSK-02), 미추천 카드 reason을 "미추천 사유 —"로 muted 표기
  - 과제 카드: 영역 Badge(neutral)·기반 과제 Badge(dark, F-TSK-04)·즉시 착수 Badge(accent, F-TSK-06)·진단 근거 인용(좌측 2px 블루 보더, F-TSK-05)·기대효과(연 절감 모노)·난이도·기간·자부담(note 각주)·매칭 솔루션 카운트만(F-TSK-01) + 이월 각주·담기 토글(담김 = primary + check 아이콘)
  - 담기 바(하단 fixed, 프로스트): 담은 과제 N개 + 제거 가능 칩 + 2~3개 권장 가이드 + 4개 이상 앰버 경고(담기는 허용, F-TSK-03) + "로드맵 보기"(1개 이상 활성) → completeStep("tasks") 후 /roadmap
  - 기반과제 연관 제안 토스트(F-TSK-04, REQ-F-15③): dependsOn의 미담긴 기반과제 감지 시 담기 바 위 토스트, [함께 담기]=addTask / [나중에]=닫기
- `app/roadmap/page.tsx` 신규 — S4 로드맵
  - 가드: selectedTaskIds 0개면 안내 + 개선 과제 링크만
  - `generateRoadmap(selectedTaskIds)` 런타임 호출(useMemo) — 하드코딩 없음
  - 상단 다크 타일 배너: goalLine + "총 N개월"(모노)만 — 중복 요약 블록 없음(F-RMP-01)
  - 단계 카드: "단계 N · 제목" + "{start+1}~{start+dur}개월차"(모노), 과제 목록(getTask — 제목·영역 Badge·기간), 자동 추가 Badge(accent)+사유(REQ-F-16), go/no-go 게이트 2열 분기(check/alert 아이콘, F-RMP-03), 자부담 밴드 모노+각주(F-RMP-04), 역할 분담 2열(F-RMP-05)
  - 단계 간 세로 연결선+chevron으로 순차 흐름 표현, 간트·정부사업 타임라인 미구현(P2 제외)
  - 말미 CTA "보고서 보기"(primary lg) → completeStep("roadmap") 후 /report
- 검증: `npx tsc --noEmit` 오류 0, `npx next build` 성공(/tasks·/roadmap 정적 프리렌더), generateRoadmap 스모크 테스트(t03·t04 담기 → 단계1에 t01 자동 삽입 + 사유, 총 5개월 / t09 담기 → t05 파일럿 자동 삽입) 통과

### 비고
- 공유 기반 파일 수정 없음, 신규 패키지 없음, 이모지 미사용
- /report는 다른 작업자 구현 중 — CTA는 링크만 연결

## 14:53 — S2 진단 결과 화면 구현

### 완료
- `app/result/page.tsx` 신규 — S2 진단 결과 (클라이언트 컴포넌트, 5섹션 F-ANL-01~08)
  - 가드: completedSteps에 "collect" 없으면 안내 카드 + "자료 정리로 돌아가기" 링크만 렌더
  - 점수는 런타임 `computeOverall(judgments)` 산출(하드코딩 없음) — 종합 57 / Lv.3 부분 적용 / ICS 45 · AOS 37.5(판정 4/5, 커버리지 80%) · OCS 55 · TAS 56.25 · FRS 81.25 · SCS 68.75. 격리 실행 스크립트로 런타임 검증, 기대값과 정확히 일치
  - 섹션 1(다크 히어로): 대형 지표는 level.label 1개만(REQ-F-10), 종합 점수는 보조 모노 "AX 준비도 57/100", 회사명(companyInput 폴백 demoCompany)·업종·인원(infoSource 각주), 3단 소견(한마디로/그래서/권고), 상위 20% 문구 + positionFootnote 각주(KSMS 계열 단일 기준, F-ANL-02)
  - 섹션 2(white): 레이더 차트 SVG 직접 구현(자사 = 블루 반투명 채움 + 블루 스트로크, 업종평균 = slate 점선 스트로크만, AXES.short 라벨, viewBox 반응형, 범례) + 축별 리스트(sortAxesByScore 오름차순 병목 우선, 점수 모노, 자사/업종 미니 바 2개 + 델타, judgedCount<totalCount 시 "판정 4/5 · 커버리지 80%" 배지, estimated 시 "추정(신뢰도 낮음)" 배지, axisFindings 근거 문장, "채점 기준·근거 보기" 접기 — 문항·판정 앵커(A값+환산점)·rationale(앵커 서술 인용)·SourceChips·보류 문항 "판정 보류 — 감점이 아닌 커버리지 하락" + deferReason). 별도 벤치마킹 섹션 없음(F-ANL-05)
  - 섹션 3(mist): areaAssessments priority 순, 등급 텍스트 Badge(심각 danger/보통 neutral/강점 success/판단 보류 outline), 0~100 숫자 미노출(REQ-F-12), asIs 한 줄 + SourceChips, hold 영역 holdReason("자료를 올리면 진단이 가능해집니다" 포함) + /collect 업로드 유도 링크, taskIds 있으면 "이 영역 개선과제 N건 보기" → /tasks?area=<areaId>(F-ANL-08)
  - 섹션 4(white, 8영역 직후): valueChainAnalysis.available 조건부 렌더, 헤더 Badge(accent) "귀사 자료로만 산출" + usedDocTypes 칩, 신호 3건 카드(warning=앰버 alert 아이콘/info=블루 info 아이콘, sourceDocIds → uploadedDocs 파일명을 label로 SourceChip 표기)
  - 섹션 5(mist): recommendationClose.title/body + strategyType(Badge accent 라벨 + 설명), CTA "개선 과제 보기"(Button primary lg) → completeStep("result") 후 /tasks 이동
- `npx tsc --noEmit` 통과 (오류 0 — 타 화면 포함 전체 무오류)

### 비고
- 공유 기반 파일 수정 없음, 신규 패키지 설치 없음, 차트는 SVG 직접 구현, 이모지·그라디언트 미사용, 다크 타일은 히어로에만 적용

## 15:04 — S5 보고서(전환) 화면 + 클라이언트 PDF 생성 구현

### 완료
- `app/report/page.tsx` 신규 — S5 보고서 ("use client", metadata 미노출)
  - 가드: completedSteps에 "roadmap" 없으면 안내 카드 + "로드맵으로 이동" 링크만 (Provider sessionStorage 하이드레이션 이후 판정)
  - 데이터 전부 런타임 계산: computeOverall(judgments) / computeRoi(selectedTaskIds) / generateRoadmap(selectedTaskIds)
  - 섹션 1(white, F-RPT-01): 요약 4칸 — ①현재 단계(Lv.3 부분 적용 + "AX 준비도 57/100" 모노) ②포지션(상위 20% 진입 목표 + ADOPTION_FOOTNOTE 축약 각주) ③예상 연 효과(만원 모노, 클릭 드릴다운: 항목·연 절감액·산출 가정 표 + 합산 + disclaimer, REQ-F-18) ④투자 회수(약 N개월 모노, 클릭 드릴다운: 총 자부담 ÷ 월 효과(연/12) 계산식 모노 블록). ③④ "산출 내역 보기" 어포던스 + chevron 회전 + aria-expanded
  - 섹션 2(mist, F-RPT-02·04): CTA 2열 — 좌 "보고서 받기"(이메일 Input @ 검증·미입력 비활성 + "PDF 보고서 받기" primary lg → PDF 즉시 다운로드 → "{email}로 보고서를 발송했습니다 (데모: 실제 발송 없음)" 완료 전환 → 후속 제안 "전문가와 결과 리뷰하기 — 30분 무료 리뷰" secondary + "PDF 다시 받기" ghost), 우 "문의하기"(구축 상담 연결 secondary lg → axcore.ai.kr/#5.contact 새 탭)
  - 섹션 3(white, F-RPT-03): 다크 타일 체험 티저 — "도입 후에는 이런 현황판을 보게 됩니다" + CSS/SVG 미니 대시보드 목업(스탯 타일·바 차트·게이지, 외부 이미지 없음) + "체험하기" ghost → "정식 오픈 예정" 안내
- `components/report/ReportDocument.tsx` 신규 — PDF용 보고서 DOM (폭 794px, 인라인 hex만 사용 — html2canvas oklch 비지원 대응, Tailwind 유틸리티 미사용)
  - 표지(로고 PNG + 회사명 + 진단일 2026-07-09 + Lv 대형 + 전략 유형, A4 1페이지 고정 높이) / 1.종합 소견 3단 / 2.6축 준비도(점수·수평 바·업종평균 마커·판정 N/M·보류·추정 표기) / 3.8영역 평가(3등급 필 배지, 숫자 점수 없음) / 4.선택 과제 N건(제목·기대효과·자부담) / 5.로드맵 단계 요약(단계·기간·과제·게이트·자동 삽입) / 6.ROI 산출 내역(항목·가정·합산·disclaimer) / 푸터(데모 고지 + 문의 URL)
- `lib/pdf.ts` 신규 — generateReportPdf: 웹폰트 로드 대기 → html2canvas(scale 2) 캡처 → A4 페이지 높이 슬라이싱 → jsPDF(A4 portrait) addImage → save("AXpoint_진단보고서_{회사명}.pdf", 파일명 금지문자 정제). 생성 중 "보고서 생성 중…" 로딩, 실패 시 오류 문구 + console.error
- 검증: `npx tsc --noEmit` 오류 0(전체), `npx next build` 성공(/report 정적 프리렌더), 브라우저 실기 구동 — 가드/드릴다운 2종/이메일 검증/PDF 다운로드/완료 상태 전환 전부 동작 확인. 생성 PDF 실측: A4 4페이지, 표지·6개 섹션·푸터 렌더 정상, 수치(연 효과 2,500만원·자부담 2,055만원·회수 10개월, 과제 4건 기준) 화면과 일치
- 버그 수정 1건: 클릭 카드 style에 `borderColor: undefined` 전달 시 React가 border-color를 초기화(currentColor)해 카드 테두리가 잉크색으로 보이는 문제 → 명시적 "var(--hairline)" 폴백으로 수정

### 비고
- 공유 기반 파일 수정 없음, 신규 패키지 설치 없음(jspdf·html2canvas 기설치분 사용), 이모지·그라디언트 미사용
- 알려진 한계: html2canvas 슬라이싱 특성상 페이지 경계에서 표 행이 잘릴 수 있음(표준 패턴 허용 범위)

## 15:13 — 통합 검증 완료 및 데모 v1 마감

### 이번 세션 전체 요약 (작업v1.md 수행)
- 기본 예시 코드·SVG 전부 제거 후 진행
- 디자인 시스템 임포트: claude.ai/design AXCORE 프로젝트(0a6dbf03) → 토큰을 `app/globals.css`에 통합, 컴포넌트 8종 `components/ui/` TSX 포팅, Paperlogy woff2 9종 `public/fonts/`, 브랜드 가이드 미러 `docs/design-system/README.md`
- 코어 로직(SSOT): `data/rubric/`(27문항·앵커·HITL 13문항·Lv 기준표·8영역 체계) + `lib/scoring/engine.ts`(결정론적 채점: 축=판정 문항 평균·결측 분모 제외·커버리지<50% 추정·종합=6축 평균) + `lib/roadmap.ts`(기반과제 자동 삽입 로드맵 생성) + `lib/roi.ts`(산출 가정 명시 ROI)
- (주)데모기업 시나리오: `data/scenario/`(문서 12건 3축 태깅·공개 6종·HITL 응답·판정 27건+근거 체인·8영역 등급·가치사슬 신호 3건·서사), `data/catalog/tasks.ts`(과제 10건). 결과: 종합 57 · Lv.3 부분 적용 · 강점 FRS 81.25/SCS 68.75 · 병목 AOS 37.5/ICS 45
- 사용자 확정 사항 반영: 단일 시나리오((주)데모기업) / OCS = "조직 준비도" / 범위 P0+P1 / 클라이언트 PDF(jspdf+html2canvas 설치 승인)
- 6단계 화면(S0~S5) + PDF 보고서 구현 (상단 각 항목 참조)

### 통합 검증 (브라우저 E2E 실기 구동)
- 랜딩 입력 → 수집 시뮬레이션 → HITL 확인 2건+설문 12문항 → 진단 결과(Lv.3·레이더·병목 정렬·8영역 등급·가치사슬) → 영역 링크로 과제 진입(?area=production 필터) → 담기 + 기반과제 연관 제안 토스트([함께 담기] 동작) → 로드맵(총 5개월·게이트·자부담 810~1,600만원) → 보고서(4칸 + ROI 드릴다운 1,950만원·회수 10개월) 전 구간 정상, 콘솔 에러 0건
- `npm run build` 성공 — 6개 라우트 전부 정적 프리렌더, `npx tsc --noEmit` 오류 0

### 수정 1건
- `app/result/page.tsx`: 8영역 "이 영역 개선과제 N건 보기" 링크로 /tasks 진입 시 "result" 단계 미완료로 가드에 걸리던 문제 → 링크 클릭 시 completeStep("result") 처리 (F-ANL-08 정식 경로 보장)

### 참고
- `npm audit` moderate 2건은 next↔postcss 기존 의존성 건으로 이번 설치와 무관 (next 업그레이드는 별도 결정 필요)
- 제외 범위: 근본원인 분석(F-ANL-06 보류), P2 기능(과제 상세 팝업·간트 바·정부사업 타임라인·과금 연동·로그인 재프레임)

## 20:14 — S0 랜딩 전면 재작성 (수정요청v1, 디자인 시스템 v2)

### 완료
- `app/page.tsx` 덮어쓰기 — 한 페이지 스크롤 → Hero + 단계 전환 위저드로 개편
  - 상태 머신 `phase: search | confirm | upload | systems` + 로그인 모달 open 상태
  - search(Hero): 밝은 캔버스 세로 중앙, "AXpoint" 브랜드 라벨(label-s·블루), 헤드라인 "3분이면 나와요, 우리 회사의 AI 도입 답안지"(display2) + 서브 "제조 기업 진단 무료 — 근거 있는 점수로 알려드려요", 중앙 pill 검색창 1개(높이 58px·max-width 560px·좌 search 아이콘·우측 안쪽 "진단 시작" 필 primary 버튼+arrow, form Enter 제출)
  - 플레이스홀더 타이핑 애니메이션: "(주)데모기업" ↔ "123-45-67890" 두 문구를 setTimeout 체인으로 한 글자씩 타이핑(110ms)→머무름(1.5s)→삭제(55ms) 반복, 끝에 "|" 커서, placeholder 속성 값 갱신 방식. 포커스/입력 시 정지(touched 플래그) 후 정적 플레이스홀더로 전환, useEffect cleanup으로 타이머 해제
  - 제출 시 useAuth().user 확인 — 미로그인이면 LoginModal(검색값 state 보존), onSuccess 시 phase confirm 진입. 로그인 상태면 바로 confirm
  - confirm(1/3): 도트 진행(●○○ + n/3 모노), "이 기업이 맞나요?" + 입력 기업명 크게 + "금속가공제품 제조업 · 광주" / [맞아요, 계속할게요] primary lg full / [다시 검색] ghost(입력 유지)
  - upload(2/3): "자료를 올려주세요", 점선 업로드 박스 클릭 → 1초 Loader("자료를 읽고 있어요") → 파일 12건 첨부(uploadedDocs 앞 4건 + 외 8건), uploadSimulated=true / [다음] primary(완료 시) / [자료 없이 진행] ghost / 지원 포맷 캡션
  - systems(3/3): "지금 상황을 알려주세요"(선택 사항 안내), 시스템 칩 5종 + 관심 영역 칩(FUNCTION_AREAS 8) → useDiagnosis update / [진단 시작하기] primary xl full · [건너뛰고 시작] ghost → companyInput 반영, completeStep("landing"), /collect 이동 / 개인정보 fine print
  - 각 phase 카드에 .ax-step-enter 진입 애니메이션(key로 재트리거) + 좌상단 chevron 뒤로 가기(호버 워시)
- v1 잔재 제거: 다크 히어로 타일·"왜 AXPOINT인가"·"진행 안내" 섹션·데모 설명 문구 전부 삭제. "공장" 단어 미사용, 해요체 통일, 이모지·그라디언트·scale 변형 없음, 시맨틱 토큰만 사용
- `npx tsc --noEmit` 통과 (전체 오류 0)

### 비고
- Hero 검색창은 Input 컴포넌트가 트레일링 버튼·높이 커스텀을 지원하지 않아, 동일 디자인 시스템 클래스(.ax-field .ax-field--pill)를 직접 조합해 구성(높이 58px 인라인 지정). 공유 파일 수정 없음, 신규 패키지 없음
- phase 상태는 세션 내 메모리 상태 — 새로고침 시 search로 복귀(companyInput·uploadSimulated·칩 선택은 sessionStorage로 유지됨)

## 20:20 — 데이터 계층 수정요청v1 개편 (공개 데이터 11종·과제 20개·설문 v2·종합 분석·용어 사전)

### 완료
- `data/scenario/publicData.ts` — 공개 데이터 6종 → 11종 재편 (sourceApi/area/digitalLevel 태깅, items 해요체 상세화)
  - 신규: pub-disclosure(전자공시), pub-venture(벤처 2012취득·2018만료), pub-innobiz(이력 없음·0건 done), pub-sns(3건 partial), pub-rnd(2024 뿌리기업 R&D 1건), pub-survey(응답 12건)
  - 제거: pub-nts(국세청) → judgments.ts FRS-02·SCS-01 evidence를 pub-disclosure로 치환
- `data/rubric/survey.ts` — v2 규칙: 전 문항 skippable, 1번 allowOther, 2번 multi 전환, 7번 "AX 프로젝트" 문구, 8번 상태 서술형 4지선다+allowOther, 13번 reasonOn:["불가"], 문항별 설계 근거 주석
- `data/scenario/hitl.ts` — 8번 응답 "장비 위주로 도입해 봤어요"로 갱신 (TAS-02 A2 "설비 위주 도입" 정합, answerDetail 유지)
- `data/scenario/judgments.ts` — TAS-04 SNS 근거 pub-sns로, TAS-03에 pub-rnd 근거 추가, "커버리지"→"자료 충분도"·"HITL 확인"→"확인 응답" 치환. 앵커·점수 무변경 (종합 57 · Lv.3 유지)
- `data/catalog/tasks.ts` — 10개 → 20개 확장. t01~t10에 subtitle(영문 태그)·beforeAfter 추가, 신규 t11~t20(전 영역 커버, 모두 미추천+진단 근거 연결 사유, t20 즉시 착수 배지 d01·d06)
- `data/scenario/areas.ts` — taskIds에 신규 과제 반영 (design→t16, cs→t17 포함 8영역 전체)
- `data/scenario/company.ts` — companyStats 7종 추가 (매출·특허·R&D·보도·고용·조달·인증, publicData sourceId 연결)
- `data/glossary.ts` 신규 — 용어 20종 + getGlossary 헬퍼
- `data/scenario/narrative.ts` — comprehensiveAnalysis 신설(결론/강점2/개선2/전략방향, AOS 37.5 vs 48·ICS 45 vs 52), recommendationClose 삭제(권고 확인 섹션 폐지), "공장"→"제조 기업" 정비
- `data/scenario/index.ts` — companyStats·comprehensiveAnalysis re-export, recommendationClose 제거
- `data/scenario/roi.ts` — t11 산출 가정 추가
- 정합성 전수 확인: evidence refId(문서 d01~d12·공개 데이터·hitl-N) 전부 유효, areas.taskIds ↔ tasks.areaId 일치

### 비고
- `npx tsc --noEmit`: 데이터 파일 오류 0. 잔여 오류 1건은 화면 파일(`app/result/page.tsx:24` — 삭제된 recommendationClose 참조)로, 화면 재작성 작업자 몫.

## 20:30 — S1 자료 정리 전면 재작성 (수정요청v1, 디자인 시스템 v2)

### 완료
- `app/collect/page.tsx` 덮어쓰기 — "정리된 자료를 보여주는 공간"으로 개편 (해석·점수·등급 미노출 유지)
  - 가드: companyInput 없으면 안내 카드 + "처음으로" 링크만 (하이드레이션 후 판정)
  - 로딩 화면(재방문 시 생략): 중앙 풀 레이아웃 — 기업 이름 크게 + 3-dot Loader + 순환 수집 메시지 4종(1.2초 간격: 공개 데이터 → 자료 읽기 → 8대 영역 분류 → 개인 정보 가림), 약 4.2초 후 본문 .ax-step-enter 전환
  - [확인이 필요해요] 최상단: 문서 확인 2건(d07·d08) 카드 문법 ①질문(title2) ②출처(파일명+문서유형 캡션) ③선택지 Tag — 선택 후 재선택 가능 + "나중에 다시 바꿀 수 있어요" 캡션
  - 설문 12문항(10번 제외 — "매출 구간은 재무정보가 확인되어 묻지 않아요" 캡션): 질문→보조설명→선택지 동일 문법, multi 토글, allowOther "기타" 칩+인라인 입력, 13번 "불가" 사유 입력, 11번 "있다" 내역 입력, 전 문항 "건너뛰기"(보류 배지 "이 문항은 판정을 보류해요"·감점 아님·재응답 가능), 진행 카운터 N/12 + "기본 응답으로 채우기" ghost(hitl.ts 응답)
  - 12문항 응답 또는 보류 완료 시 surveyDone=true → 섹션 자동 접힘("확인 완료 — 응답 N건 반영" 요약, 다시 펼치기 가능)
  - 공개 데이터: 컴팩트 행 리스트(종류 label-m · 출처 캡션 · 건수 모노 우측), partial은 건수 옆 warning 점, 0건도 그대로 표기, 행 클릭 시 items 상세 접기 — 개수 단어("N종") 미사용
  - 8대 영역 자료 분류(구 "업로드 자료 분류"+"커버리지" 통합): FUNCTION_AREAS 8개 아코디언(자료 많은 상위 3개 기본 펼침), 그룹 헤더 = 영역명 + 업로드 N·공개 M(모노) + 부족 영역 warning Badge("자료가 부족해요") + missingHint 유도 캡션. 업로드 행 = 파일명 + 디지털화 수준·문서 유형 태그만(신뢰도·태그 수정·확장자 태그 제거, 개인정보 가림 배지 유지) + "상세" 접기(extractedDetail+summaryTeaser). 공개 행 = 종류명 + 디지털화 수준 + 수집처 출처 태그
  - CTA "진단 결과 보기" primary xl — 문서 2건+설문 완료 시 활성, 미완료 시 disabled + "위 확인을 마치면 진단으로 넘어갈 수 있어요" → completeStep("collect") 후 /result
- 디자인 v2 준수: --bg-base 연속 흐름(풀블리드 타일 제거), max-width 980px, 다크 타일·이모지·scale·그라디언트 없음, primary 버튼 화면당 1개, 해요체 통일, 내부 용어 미노출
- `npx tsc --noEmit`: app/collect/page.tsx 오류 0

### 비고
- 잔여 타입 오류 1건은 기존 건 그대로: `app/result/page.tsx:24` (삭제된 recommendationClose 참조) — S2 화면 재작성 몫
- 재방문 시 문서 확인 선택값·설문 응답은 시나리오 기본 응답으로 복원 표시(세션 상태에는 완료 여부만 저장됨)

## 20:31 — S3 개선 과제·S4 로드맵 전면 재작성 (수정요청v1)

### 완료
- `app/tasks/page.tsx` 덮어쓰기 — S3 개선 과제 후보 (참고 UI: 개선과제.png)
  - 헤더 카드: "개선 과제 후보" h2 + 해요체 설명 + 큰 숫자 요약 3개(20개 후보 과제 · 3곳 우선 개선 영역 · 4개 보유 자료로 즉시 착수, 모노 숫자) + 스마트공장 표준 일정 각주
  - 필터 칩 행: [전체 20] + 8영역 칩(영역명+모노 카운트, ?area= 초기 반영) + 우측 "정렬 · 추천순" 정적 캡션
  - 카드 그리드 3열(980px 이하 2열, 640px 이하 1열): 영역 Badge + 보조 Badge(추천/즉시 착수/기반 과제) + 우상단 원형 체크(카드 전체 클릭 토글) + title1 제목 + 영문 subtitle(대문자·자간) + summary 2줄 + beforeAfter 블루 라인 + 시계 아이콘 예상 기간 + 하단 착수 조건 1줄(feasibility 배지 문구 success / dataRequirements[0] muted), 미추천 카드는 reason 하단 캡션
  - 더보기 제거 — 20종 전부 그리드 노출(추천순 정렬: recommended 우선 → buildOrder)
  - 추천 5종 미리 담아두기: sessionStorage 복원 후("result" 완료 확인 시점) 1회, tasks 미완료·선택 0개 조건
  - 하단 고정 바: grey-800 radius 16 떠 있는 바 — "과제 N개 담음"(모노 N) + 부제(0개/2~3개 안내) + 4개 이상 앰버 경고 + [이 과제로 로드맵 보기] primary(0개 disabled) → completeStep("tasks") 후 /roadmap
  - 기반과제 연관 제안 토스트 유지(grey-800 토스트 문법, [함께 담기]/[나중에])
- `app/roadmap/page.tsx` 덮어쓰기 — S4 실행 로드맵 (참고 UI: 로드맵 참고ui.png, 세로 타임라인)
  - 다크 배너 제거 → 라이트 헤더: "실행 로드맵" h2 + goalLine body1 + 요약 행(담은 과제 N개 · 총 M개월 · 예상 자부담 X~Y만원, 모노)
  - 세로 타임라인: 좌측 레일(세로 라인 + 단계 도트 + "1~2개월차" 모노 마커) + 우측 단계 카드, 단계별 도트·좌측 보더 블루 농도 변화(blue-500 → blue-100 → grey-300)
  - 단계 카드: "단계 N · 제목" + 기간 모노 / 과제 리스트(제목·영역 Badge·기간, 자동 추가 accent Badge+사유) / 진행 판정 게이트(기준 목록 + 충족/미달 2톤 분기) / 예상 자부담 모노+각주 / 역할 2열(귀사·AXpoint)
  - 말미 CTA: "보고서 보기" primary xl → completeStep("roadmap") 후 /report
- `npx tsc --noEmit` — 두 파일 오류 0

### 비고
- 타 파일 기존 오류 1건 확인(수정하지 않음): `app/result/page.tsx(24,3)` — `@/data/scenario/narrative`에 `recommendationClose` export 없음
- `lib/roadmap.ts`의 goalLine("…스스로 흐르는 공장…")은 공유 파일 수정 금지 원칙에 따라 그대로 표기 — "공장→제조 기업" 라이팅 규칙 적용하려면 공유 데이터 측 수정 필요

## 20:41 — S5 보고서 화면·PDF 전면 재작성 (수정요청v1, 디자인 시스템 v2)

### 완료
- `app/report/page.tsx` 덮어쓰기 — S5 보고서(전환)
  - 헤더: "{기업명} 진단 결과 요약" + "진단일 2026-07-09 · 담으신 과제 N건 기준" 캡션
  - 요약 4칸(라이트 카드): ① 현재 단계 — Lv 라벨 크게 + level.description만("AX 준비도 57/100" 보조 표기 삭제) ② 포지션 — overall.industryDiff 런타임 계산 "업종 평균보다 +2.3점"(모노, 양수 블루) + "중소 금속가공 표본 평균 55점 대비" 캡션 ③ 예상 연 효과(모노 만원 + 산출 내역 드릴다운) ④ 투자 회수(약 N개월 + 계산식 드릴다운)
  - CTA 버튼만 2개: [보고서 받기 →] primary xl(모달) + [문의하기 →] secondary xl(axcore.ai.kr/#5.contact 새 탭), 화살표는 Icons.arrow 라벨 끝
  - 보고서 받기 모달: 이메일 @ 검증 → [PDF 받기] → 생성·다운로드 → 완료 상태 "{email}로 보냈어요 (데모라 실제 발송은 없고, PDF가 다운로드됐어요)" + [전문가와 결과 리뷰하기] secondary(새 탭)
  - 체험 티저: 다크 타일 제거 → 흰 카드+헤어라인 안 라이트 톤 미니 SVG 대시보드, [체험하기] → https://axcore.it.kr 새 탭(rel noreferrer)
- `components/report/ReportDocument.tsx` 덮어쓰기 — 페이지 단위 상세 보고서(각 794×1123px, padding 56px, `data-report-page`, 하단 페이지 번호·AXpoint 표기)
  - 페이지 구성: ①화이트 표지(AXpoint 로고 텍스트+기업명+진단일+Lv 크게+전략 유형 배지, 점수 미표기) ②종합 소견 3단+업종 대비 포지션(industryDiff 문구+비교 바) ③카테고리별 준비도 6축 표(점수 바·평균 마커·보류·자료 충분도·추정) ④기능영역 8곳 등급 표("관리 대상"/보통/강점/판단 보류 — 숫자 없음)+가치사슬 신호 요약 ⑤담은 과제(제목·beforeAfter·기간·자부담, 6건 초과 시 페이지 분할) ⑥실행 로드맵(기간·게이트·역할) ⑦예상 효과 산출 내역+말미 고정 블록 "이 진단이 본 자료·방법"(업로드 12건·공개 데이터 11종·확인 응답 12건·6축 27문항 — 전부 데이터 배열 length 런타임 참조)+한계 고지
  - 낙인 어휘 교체: 8영역 "심각" → "관리 대상" / 인라인 hex만 사용(html2canvas 제약 유지)
- `lib/pdf.ts` 재작성 — 긴 캔버스 슬라이싱 폐기 → `[data-report-page]` 요소를 페이지별 html2canvas 캡처 후 jsPDF addPage 삽입(표·행이 페이지 경계에서 잘리지 않음), 파일명 "AXpoint_진단보고서_{회사명}.pdf" 유지
- `npx tsc --noEmit` 오류 0 · `npx next build` 성공(11/11 정적 생성)

### 비고
- `lib/roadmap.ts`의 goalLine("…스스로 흐르는 공장…")은 "공장" 라이팅 규칙 위반 소지가 있어 PDF 로드맵 페이지에서 미표기(공유 파일 수정 금지 원칙) — 총 기간·단계 정보만 표기
- 이전 기록의 `app/result/page.tsx` recommendationClose 오류는 현재 tsc 기준 해소 확인

## 20:36 — S2 진단 결과 화면 전면 재작성 (수정요청v1)

### 완료
- `app/result/page.tsx` 덮어쓰기 — 삭제된 `recommendationClose` import로 인한 tsc 오류 해소
  - 가드(collect 미완료 시 안내+링크) + 최초 진입 약 2초 로딩("방금 정리된 자료를 기준으로 진단하고 있어요", result 완료 재방문 시 생략) 후 `.ax-step-enter` 본문
  - 섹션 1: 라이트 카드 기업 개요(대표·설립·업력·소재지·infoSource) + companyStats 가로 스크롤 칩(우측 원형 스크롤 버튼, 클릭 시 상세 Modal + sourceApi 출처) + DotStepper 현재 Lv.3 → 목표 Lv.4(업종 평균 Lv.3 캡션 = INDUSTRY_AVG→mapLevel 계산, KSMS TermTooltip) + 종합 소견 3단 카드
  - 섹션 2 "카테고리별 준비도": 레이더(꼭짓점 호버 점수 툴팁·축 라벨/도트 클릭 선택) + 우측 카드(기본 요약/카테고리 상세/모두 보기 그리드), 프로그레스바 1개 통합(평균 위치 세로 점선 마커), "점수 근거" Modal(문항·판정 앵커 인용·rationale·출처 텍스트·판정 N/M·자료 충분도 % 팝업 전용)
  - 섹션 3: 8영역 콤팩트 그리드(관리 대상/보통/강점/판단 보류, asIs 2줄 클램프, 숫자 점수 미노출), 관리 대상 "사유 보기" Modal = 장비.png 인과 체인(production/logistics/quality 로컬 상수), hold 업로드 유도, 개선과제 N건 링크(completeStep)
  - 섹션 4: 가치사슬 연결 칩 행(발주서=소요 + 생산 기록=투입·소진 + 재고표=잔량 → 소진·회전 신호) + 품목별 표 5행(기어 D-3·샤프트 B D-5 즉시 발주 danger / 브라켓 B-102 회전 0.3회 과잉 주의, vc2 정합) + 기존 신호 3건 카드(출처 텍스트)
  - 섹션 5: 종합 분석 결과(결론/강점 2/보완 2/AX 전략 방향 + 전략 유형 배지) + CTA "개선 과제 고르러 가기"(primary xl → completeStep("result") 후 /tasks). 권고 확인 섹션 폐지.
  - TermTooltip 6곳(KSMS·안전재고·월 회전(재고 회전)·발주점·판정 보류·데이터 관리 체계/예지보전/OEE)
- 점수 전부 `computeOverall(judgments)` 런타임 계산 — 실행 검증: 종합 57(raw 57.29)·Lv.3 부분 적용, ICS 45 / AOS 37.5(4/5, 충분도 80%) / OCS 55 / TAS 56.25 / FRS 81.25 / SCS 68.75, 강점 FRS·SCS / 병목 AOS·ICS, 업종 평균 55 → Lv.3
- `npx tsc --noEmit` 통과 (오류 0, 타 파일 포함)

### 비고
- 공유 파일 수정 없음, 신규 패키지 없음. 인과 체인·재고 표는 화면 로컬 상수(시나리오 정합).

## 20:48 — 수정요청v1 전면 개편 완료 및 통합 검증

### 이번 세션 요약 (docs/수정요청/수정요청v1.md 수행)
- **디자인 시스템 v2**: 메인 컬러(#0A50FF)·Paperlogy만 유지하고 토스 문법으로 전면 개편 — 쿨그레이 뉴트럴(순수 블랙 금지), 시맨틱 토큰 라이트/다크 2세트(`[data-theme="dark"]` 구조만, 라이트 고정), 라운드 사다리, 낮은 알파 섀도 3단, 모션 토큰(120/200/320ms), 프레스=오버레이(scale 제거), 호버 워시, 얇은 선택 스타일. Modal/Toast/Loader(3-dot)/TermTooltip/DotStepper 신규
- **크롬**: 헤더바 삭제 → 1~6 화살표 스텝 헤더로 교체, Next devIndicators(플로팅 버튼) 제거
- **인증**: components/auth + app/auth(login·signup) — 가짜 인증(디폴트 값 채움), 랜딩 검색 입력 시 로그인 팝업(검색 내역 유지)
- **데이터 v2**: 공개 데이터 11종(출처 API 표기), 설문 규칙 v2(전 문항 SKIP·기타·복수선택·사유 입력·"AX 프로젝트"), 과제 카탈로그 20종(영문 태그·Before→After), 기업 개요 통계 7종, 용어 사전 20종(data/glossary.ts), 종합 분석 결과(comprehensiveAnalysis) 신설 — 판정·점수 무변경(종합 57·Lv.3 유지)
- **화면 v2** (상단 20:14~20:36 항목 참조): S0 히어로+타이핑 플레이스홀더+3단계 위저드 / S1 로딩·확인 UX·공개 데이터 표·8대 영역 자료 분류 통합 / S2 기업 개요·단계 스텝퍼·인터랙티브 레이더·점수 근거 팝업·관리 대상 인과 체인·가치사슬 표·종합 분석 / S3 참고 UI 카드+추천 미리 담기 / S4 세로 타임라인 / S5 4칸(점수 삭제·업종 대비 포지션)+버튼 2개+PDF 페이지 단위 렌더(7p, A4 흐름 유지)
- 라이팅 규칙 전면 적용: 해요체, "공장"→"제조 기업"(스마트공장 고유명사 제외), 심각→"관리 대상", 커버리지→"자료 충분도", HITL→"확인 요청", 데모 설명 문구 제거(보고서 발송 안내만 예외)
- 수정 1건(직접): lib/roadmap.ts goalLine "공장"→"현장" 및 해요체 전환

### 통합 검증 (프로덕션 빌드 + 브라우저 E2E)
- `npm run build` 성공 — 8개 라우트(auth 포함) 정적 생성, `tsc --noEmit` 오류 0
- E2E: 검색 입력→로그인 팝업(디폴트 값)→기업 확인→업로드(1초 로딩)→현황→수집 로딩(기업명+순환 메시지)→확인 요청(문서 2건+기본 응답 채우기, 10번 미노출)→공개 데이터 표(11종·0건 포함)→진단 로딩→기업 개요 칩·Lv 스텝퍼(현재 Lv.3→목표 Lv.4·업종 평균)→레이더 축 클릭·도트 툴팁·점수 근거 팝업(판정 4/5·자료 충분도 80%)→가치사슬 표(D-n·권장 조치)→종합 분석→과제(추천 5개 자동 담김·하단 바)→로드맵(타임라인·게이트·자부담 1,610~3,100만원)→보고서(포지션 +2.3점·효과 2,500만원·회수 12개월)→PDF 다운로드(7페이지 A4) 전 구간 정상, 콘솔 에러 0건
- 참고: 검증용 프로덕션 서버(`npm run start`)가 3000 포트에 실행 중
