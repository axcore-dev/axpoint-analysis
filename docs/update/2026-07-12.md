# 2026-07-12 작업 기록

## 18:38 — 수정요청v4 전면 반영

### 공통
- 호버 설명 툴팁(.ax-tooltip): 반투명 회색(grey-700 86% + blur)으로 교체, 흰 텍스트 — 읽기에 방해되지 않는 선
- footer 로고: 깨짐 수정 — 원본 비율 유지하며 크게(min(420px, 100%), height auto)

### 자료 올리기
- 카드 모달 중앙 정렬: 컨테이너 items/justify-center → 카드 margin:auto 방식으로 교체 — 카드가 뷰포트보다 길어져도 상단이 잘리지 않는 안전한 중앙 정렬

### 자료정리
- 자료 확인(설문): 확인 완료 후 재진입 시 카드 좌측 상단에 뒤로(<) 옆 **앞으로(>) 버튼** 추가(`ForwardIconButton` 신설) — 원하는 구간 빠른 이동. 문서→설문→완료 카드 순 이동(마지막은 재로딩 없이 완료 카드 복귀)
- FlowStepper 번호 원 콤팩트화: 32px → 24px, 폰트 축소
- 자료 분류: 헤더에 기업 이름 추가("데모기업에 대해 총 48건을 수집했어요")
- 8대 영역: 패널 높이 고정(460px ≈ 자료 10행) — 개수와 무관하게 일정, 초과분은 내부 스크롤(스크롤바 숨김). '디지털화 수준' 라벨을 pill(배경+굵기)로 강조

### 진단 결과
- 현재 단계: `현재`(라벨) → `Lv.3 부분 적용`(display2 크게, Lv.3 브랜드 컬러) → `업종 평균(Lv.3)과 비슷한 수준이에요`(서브) 구조로 변경
- "지금 당장은" 별도 카드로 분리 — 과제는 **품목 코드 표준화 · 재고·발주점 자동 알림**으로 교체(모바일 작업일보 제외 요청 반영)
- 레이더 호버 툴팁: 2줄(자사 39 / 평균 54), 평균은 회색 저채도
- 카테고리 재클릭 시 선택 해제
- 모두 보기: AX진단 결과 상세 카드가 사라지고 6개 카드가 그리드로 직접 노출(박스인박스 제거)
- 상세(요약 상태): 종합 점수 vs 업종 평균 비교(숫자 + ScoreBar) 추가
- 기능영역 팝업: "— 왜 관리 대상일까요" 문구 제거(영역명만)
- 가치사슬 신호: ① 아코디언 내부 상단에 "올려주신 자료를 결합했어요 [발주서]+[생산일지·작업일보]+[재고표]+[거래명세서] → 품목별 재고 흐름" 시각화 행 추가 ② 태그 호버 시 근거가 되는 표의 행을 브랜드 컬러로 하이라이트(STOCK_ROWS에 signals 매핑: 브라켓 B-102 ← vc1·vc2, 기어 D·샤프트 B ← vc3)
- 종합 분석: 불릿에 동그라미(그룹 컬러) 표시 + '—' 연결 대신 제목/본문 개행

### 개선 과제
- 카테고리 칩 8대 기능영역으로 원상복귀(★추천 맨 앞 유지), 카드 뱃지도 영역명 복귀 — 과제 내용(방법론 정합)은 유지
- "우선순위 높은 2~3개를 권해요" 경고를 "과제 N개 담음" 바로 오른쪽으로 이동

### 로드맵
- '경영문제 정의 완료' 단계 제거 — 로드맵은 담은 과제로만 구성, 단계 번호가 곧 우선순위(1부터 재번호). generateRoadmap·페이지·PDF 정합

### 보고서
- 브랜드 컬러 적용: 현재 단계(Lv.3 부분 적용) · 포지션(+2.3점) · 예상 연 효과 금액 · 투자 회수 개월
- 포지션: "업종 평균보다" 프리픽스 제거 → 점수 오른쪽에 작은 괄호 "(업종 평균보다)"
- 산출 내역 보기: 회색 컬러 + → 화살표(기존 브랜드 컬러·▼ 제거)

### 검증
- `tsc --noEmit` 0건, `npm run build` 통과(11페이지)
- 브라우저 E2E: footer 로고 선명 → 위저드(중앙 정렬) → 자료 정리(콤팩트 스텝퍼·기업명 헤더·수준 pill·고정 높이·앞으로 버튼) → 진단 결과(큰 레벨 표기·지금 당장은 카드·툴팁 2줄·모두 보기 6카드·종합vs평균 바·태그 호버 행 하이라이트·불릿 개행) → 개선 과제(8대 영역 칩·경고 인라인) → 로드맵(완료 카드 없음·단계 1부터) → 보고서(브랜드 컬러·괄호 표기·회색 산출 링크) — 전 구간 정상, 콘솔 에러 0건

## 18:48 — 자료 올리기 카드·타이포 확대, footer 전폭 (추가 요청)

- 위저드 카드 사이즈 10% 확대: maxWidth 520 → 572, 패딩 32 → 36. 위치는 중앙보다 살짝 위(top −28)
- 자료 올리기 페이지 타이포 20% 확대 — 페이지 스코프(.axp-landing)로 타입 토큰 재정의(display2 40→48, h3 22→26, body2 15→18, label-m 15→18, caption 12→14 등). 검색바: 높이 58→66, 입력 폰트 16→19, 아이콘 22, 진단 시작 버튼 md→lg(높이 50), 히어로 컨테이너 560→640
- footer 콘텐츠 가로 전폭 사용: container-wide(1200px) 제한 제거, 좌우 패딩 40px
- 검증: `tsc`·`npm run build` 통과, 브라우저에서 히어로/확인 카드/업로드 카드/전폭 footer 확인

## 19:39 — 수정요청v5 전면 반영

### 공통 — 타이포 전면 확대
- 전역 타입 램프 약 15% 확대(globals.css): 본문 15→17px, h2 24→28, h3 22→25, title1 18→20, caption 12→13 등 — 헤더바·진단 결과·자료정리 등 토큰 사용 전 화면 반영 (PDF는 고정 px라 영향 없음)
- 헤더바(StepBar): 라벨 13→15px, 번호 배지 18→21px
- 자료 올리기 카드: 더 확대(maxWidth 572→640, 패딩 40) + 위치 15% 추가 상향(top calc(-28px − 10vh))

### 자료정리
- 안내 카피: "확인 2건과 짧은 설문이 있어요" → "확인 2건과 {N}건 설문이 있어요" (N = 노출 설문 수, 현재 11)

### 5단계 실행 페이즈 재편 (방법론 7단계 → 표현 5단계)
- `data/catalog/method.ts`에 ROADMAP_PHASES 신설: 1 경영문제 정의 → 2 데이터화·표준화 → 3 생산 모니터링 → **4 운영 안정화(재고·물류/품질/설비 — 기업 맞춤 선택, optional)** → 5 공정 최적화·확산
- 과제 데이터의 methodStep(2~7)은 유지, 로드맵 생성기(lib/roadmap.ts)가 페이즈로 그룹 — 4단계 내 과제 순서는 AX 실행 우선순위(재고·물류 → 품질 → 설비). 4단계 purpose에 "(선택 진행)" 표기
- 화면·PDF 로드맵 단계 번호는 order(우선순위) 기준으로 통일

### 진단 결과
- Key Point 카드: "품목 코드 표준화 · 재고·발주점 자동 알림"만 남기고 중앙 정렬·크게(h3). 문구는 narrative.ts `keyPoint`로 공유(화면·PDF 정합)
- 카테고리 섹션 타이틀 — 종합 점수 구간별 3종: <45 "기초 체력부터 — 기반을 만드는 단계예요" / 45~65 "평균권이에요 — 병목을 채우면 앞서갈 수 있어요" / >65 "상위권이에요 — 강점을 확장할 차례예요"
- 모두 보기: 6개 카드 1열 구성. 요약 카드의 부가 설명("— 여기부터 채우면 효과가 빨라요." / "— 도입을 감당할 체력이에요.") 삭제

### 로드맵
- 할 일: 귀사/AXpoint 태그 pill → 도트 불릿 + 주체 볼드

### 보고서
- 드릴다운 카드 호버 시 '산출 내역 보기' 브랜드 컬러 전환(.axp-drill 스코프 CSS)
- **PDF 정합(진단 내용 = 보고서 내용)**:
  - 표지: "by 주식회사 에이엑스코어"로 수정, "지금 당장은 — {keyPoint}" 라인 추가
  - 1p 종합 소견(구 overallOpinion 3단) → 화면과 동일한 **종합 분석 결과**(결론 + 강점/보완/AX 전략 제안 불릿)로 교체
  - 가치사슬 신호를 별도 페이지로 분리하고 **품목별 재고 흐름 표** 추가 — 표 데이터는 `data/scenario/valueChain.ts` stockFlowRows로 이동(화면·PDF 단일 소스)
  - 섹션 재번호(1 종합 분석 ~ 8 산출 내역, 총 8p), 로드맵 5단계 페이즈 반영

### 검증
- `tsc --noEmit` 0건, `npm run build` 통과. 브라우저 E2E: 확대 타이포·카드 위치 → "확인 2건과 11건 설문" → 결과(Key Point 중앙·구간 타이틀·모두 보기 1열·부가 설명 삭제) → 로드맵(단계 1 데이터화·표준화 / 단계 2 운영 안정화 통합·선택 진행·도트 불릿) → 보고서 화면 + PDF DOM 8페이지 전체 텍스트 정합(by 주식회사·Key Point·종합 분석·재고 흐름 표) 확인, 콘솔 에러 0건

## 19:53 — v5 보완: 5단계 재편 적용 위치 정정(로드맵 → 진단 결과 Lv) 외

- **5단계 재편 적용 위치 정정**: 로드맵에 잘못 적용했던 5단계 페이즈를 원복(lib/roadmap.ts — v4의 방법론 단계(2~7) 그룹으로 복귀, method.ts의 ROADMAP_PHASES 제거)하고, **진단 결과의 Lv 체계(data/rubric/meta.ts LEVELS)에 적용**:
  - Lv.1 경영문제 정의 / Lv.2 데이터화·표준화 / Lv.3 생산 모니터링 / Lv.4 운영 안정화(재고·물류/품질/설비 선택) / Lv.5 공정 최적화·확산
  - 점수 구간은 기존 유지(57점 → Lv.3, 회귀 기준 정합). 데모기업 표기: "현재 Lv.3 생산 모니터링 단계예요". 결과 스텝퍼·보고서 요약·PDF 표지에 자동 반영
- **DotStepper 도트-라벨 정렬 수정**: 라벨을 각 도트 위치(i/(n-1))에 절대 배치 — 도트 바로 아래 정렬(첫/끝은 좌/우 정렬)
- **Key Point 카드**: 상단 중앙에 '시작점' 라벨 추가
- **카테고리 타이틀**: 고정 구간(45/65) → 업계 평균 ±10 동적 구간 + 직관 문구("57점은 업계 대비 평균 점수예요" / "...보다 낮은·높은 점수예요")
- 검증: `tsc`·`npm run build` 통과. 브라우저 — 결과(Lv.3 생산 모니터링·새 스텝퍼 라벨 정렬·시작점·직관 타이틀), 로드맵(단계 1 데이터화·표준화 / 단계 2 품질 안정화 / 단계 3 재고·물류 운영 안정화로 원복) 확인
- **개선과제 카드 노출 문자열 정리**: 착수 조건·사유에 남아 있던 내부 ID(t01~t21, d06·d07) 전부 과제명으로 교체("(t11 선행 필수)" → "(설비 IoT 모니터링 선행)" 등). 어휘 통일 — 사유는 해요체·"권해요"로, 착수 조건 괄호는 "(보유)/(선행)/(선행 필수)/(업로드 필요)" 문법으로. 전체 20종 카드 브라우저 확인

## 20:12 — 더미 데이터 현실화 · DART 출처 스타일 · 방법론 과제 보강

### 더미 데이터 현실화 (업계 대부분 Lv.1~2 전제)
- **판정(27문항)·축 점수는 사실 기반이라 유지**하고, 레벨 구간과 업계 평균을 현실화:
  - LEVELS 구간 재설계: Lv.1 [0,30] / Lv.2 [31,60] / Lv.3 [61,75] / Lv.4 [76,88] / Lv.5 [89,100] — **데모기업 57점 → Lv.2 데이터화·표준화** (수기 4회 재입력·코드 불일치 시나리오와 서사 정합), 목표 Lv.3 생산 모니터링
  - INDUSTRY_AVG 하향: ICS 50 / AOS 46 / OCS 45 / TAS 44 / FRS 55 / SCS 52 (평균 48.7 → Lv.2) — 데모기업은 평균 +8.6점(±10 이내 "업계 대비 평균 점수" 구간)
  - 정합 갱신: narrative 축별 평균 수치 4곳(55/52/46/50점), positionFootnote·PDF 각주를 KSMS → AX 실행 5단계 자체 기준으로, judgments·narrative 주석(Lv.2 기준)
- **회귀 기준 변경**: 종합 57점 · Lv.2 데이터화·표준화 · 업종 평균 48.7(Lv.2) — 이후 시나리오 수정 시 이 기준으로 정합 확인

### 진단 결과 — 기업 정보 출처 스타일
- "기업 정보 기준: DART 전자공시" 인라인 텍스트 → 카드 우측 상단의 출처 칩(🌐 "출처 · DART 전자공시 (데모 데이터)", 회색 pill)으로 이동

### 개선 과제 — 방법론 주요 과제 보강 (20 → 22종, 중복 제외)
- v5 방법론 문서의 실무 과제를 검토해 **중복이 아닌 2종만 추가** (사용자 지시: 중복 제외):
  - t22 **하드카피 문서 DX** (종이 문서 데이터화 — 과거 기록 자산화) — 경영지원, 2단계
  - t25 **숙련자 암묵지 DX** (노하우 구조화) — 생산관리, 2단계, 작업표준서 갱신 연계
- 중복 판정으로 미추가: **수기 업무 DX** — 모바일 작업일보(t03)·검사성적서 전자화(t05)·유지보전 전산화(t12)가 이미 분담 / **노후 설비 데이터 네트워크화** — 설비 IoT 모니터링(t11)과 실질 동일 → t11 summary에 "노후 설비까지 센서·게이트웨이로 연결" 명시로 흡수
- areas.ts 영역별 taskIds 반영 (경영지원 3·생산관리 5)

### 검증
- `tsc`·`npm run build` 통과. 브라우저 — 결과(Lv.2 데이터화·표준화 / 업종 평균 Lv.2 / 종합 57 vs 평균 49 비교 바 / "57점은 업계 대비 평균 점수예요" / DART 출처 칩 우측), 개선 과제 카탈로그 22종 확인
