# 2026-07-29 작업 기록

## 01:53 — 검색 자동완성 구현 + 진행 단계 영속화 + 전 화면 커밋

- **검색 자동완성 미작동 원인 2건 수정**
  - 검색 API가 로그인 필수 → `/api/companies/search`만 공개 전환(verify는 인증 유지). 비로그인 랜딩에서 자동완성 동작 (`212dd3d`)
  - 비즈노 외부 검색이 기업명 질의에 항상 0건 — `gb=1`이 사업자번호 전용 파라미터. 제거하니 이름·번호 모두 검색됨(실측: 화천기공 10건) (`da0e2c7`)
- **자동완성 디자인 반영**: 항목에 건물 아이콘 + 기업명 매칭 하이라이트 + 사업자번호 모노 배지, 포커스 항목엔 지역·업종·설립 상세줄(데이터 있을 때만). 검색 응답에 region·industry·estDate·address 추가. 기업 확인 카드 사업자번호도 하이픈 표기 통일
- **진행 단계 영속화**: `completeStep`이 메모리에만 기록돼 마이페이지 재열람·이어하기가 항상 가드에 막히던 버그 → PATCH로 `completedSteps` 서버 저장 (`8f4606f`). 재열람 시 "보고서 보기" 정상 동작 확인
- **UI 복구(에이전트 3개)**: collect(수집 헤더·분류 그리드), result(현재 단계·레이더·8영역·종합 분석), tasks(추천·칩·카드·담기 바·연관 토스트), roadmap(타임라인·STEP 카드), report(요약 카드·CTA·체험 티저) — 원본 레이아웃·문구 유지 + 서버 데이터 연결, 서버 미구현 데이터는 조건부 생략, 가치사슬 제외(확정) (`d948564`)
- **브라우저 검증**: 자동완성 → 기업 확인 → collect → 재판정 → result → tasks(담은 2개 서버 복원) → roadmap → report → mypage 재열람 전 구간 완주, 콘솔 오류 없음
- **/security-review(api develop 신규 커밋)**: NO FINDINGS — 검색 공개 전환은 공개 등기 정보만 반환, 파라미터 바인딩·인가 범위 확인
- 실수 기록: 브라우저 멈춤(이탈 경고 다이얼로그) 조사 중 잘못된 명령으로 Chrome 전체를 강제 종료 — 즉시 재실행, 세션·기록 무손실 확인
- 커밋: front `e5b5a4c`(인증·API 클라이언트) `8f4606f`(플로우 영속화) `31b25e2`(랜딩) `d948564`(5화면) `d3d99f3`(마이페이지) `bf9b1de`(지침 문구) / api `212dd3d` `da0e2c7`

## 08:55 — 결과 화면 원본 UI 추가 복구 (사용자 캡처 기준)

- **Lv.1~5 단계 스테퍼**: 결과 API에 레벨 전체 명칭(levels) 추가, DotStepper로 현재(링)·목표 단계(+1) 시각화 — 캡처와 동일한 단계 명칭(시드 기준)
- **레이더 5축 고정**: axes를 축 카탈로그 LEFT JOIN으로 변경 — 미판정 축도 항상 5축 반환, 프론트는 0점 처리. 판정 축이 2개뿐이어도 레이더 렌더
- **기업 개요·통계 칩**: 결과 API에 company(기업명·사업자번호·대표·지역·설립일·고용·연매출) 추가. 상단 카드에 기업명+사업자번호, 대표·설립(업력)·지역 줄, 통계 칩 행(가로 스크롤) — 값이 수집된 항목만 표시(현재 verify는 이름·번호만 저장하므로 외부 데이터 연동 전까지 대부분 비어 있음)
- 축 클릭 상세(75/100점·ScoreBar·판정 5/11)·툴팁·페이드 등 원본 상호작용 동작 확인
- 커밋: front `0b6a396` / api `436cda3`

## 15:38 — 서버 배포 연동: edge 네트워크·다중 CORS·저장소 정비 (서버 에이전트 협업)

- **서버 장애 대응 반영**: 구/신 스택 서비스명(api) 충돌로 Caddy DNS가 섞이던 장애 — 서버 에이전트가 공용 `edge` 네트워크+고유 별칭(axpoint-api/axpoint-new-api)으로 해결. 로컬 반영:
  - api 레포 `docker-compose.prod.yml`: api 서비스 edge 참여+별칭, networks 블록 (`132ee3f`)
  - prototype-axpoint-api 레포: 서버 상태의 Caddyfile(도메인 2개 라우팅)·compose 동기화 커밋·푸시 (`1d1cadb`) — 서버에서 `git checkout -- Caddyfile docker-compose.prod.yml && ./deploy.sh` 실행 필요
- **FRONT_ORIGIN 콤마 구분 다중 출처 허용** (`85e0270`): CORS(index.ts)+trustedOrigins(auth.ts) — 정식 도메인(axcore.io.kr)과 Vercel 데모 동시 운영용. 보안 리뷰 NO FINDINGS(hono/cors 정확 일치·better-auth 완전 일치·edge로 데이터 계층 비노출 검증)
- **api 저장소 정비** (`6d18b13`): CLAUDE.md 백엔드 맞춤 재작성(깨진 include 제거·개발 명령·env 규칙), .gitignore 보강(deploy_key*·인증서·로그·.claude/)
- **배포**: PROD_ENV_FILE 시크릿 갱신(사용자) 후 develop→main ff 푸시로 자동 배포 트리거. 도메인 확정: 프론트 axcore.io.kr(현재 Vercel 데모), API api.axcore.io.kr. 배포 반영은 Vercel 출처 preflight 허용 여부로 검증
- 남은 것: Resend에 axcore.io.kr 도메인 검증(사용자), 서버 PAT 재발급(사용자), Caddy 독립 스택 분리(서버 에이전트 권고 — 배포 안정화 후 별도 작업)

## 16:42 — 신규 백엔드 운영 배포 성공 (자동 배포 파이프라인 가동)

- Actions 실패 원인 규명·해결: ① DEPLOY_KNOWN_HOSTS 불일치(주석 줄 입력 → 서버 스캔값 3줄로 교체) ② 서버 authorized_keys에 배포 공개키 미등록(서버 에이전트 등록). 서버 remote는 PAT 평문 노출 제거 후 읽기 전용 deploy key(SSH)로 전환됨
- 배포 반영 검증(외부): api.axcore.io.kr health 200, 공개 검색 실데이터 응답(신규 코드 확정), CORS 다중 출처 동작 — Vercel 데모·axcore.io.kr 허용, 미허용 출처 차단, 구 api.axcore.kr 200 무영향
- Resend axcore.io.kr 도메인 Verified(사용자), DEPLOY.md에 deploy key 방식·시크릿 함정 반영(`f9c33dd`)
- 이후: git push origin main만으로 서버 반영되는 자동 배포 체계 완성. 남은 것 — 서버 내부 사후 점검(Resend 새 키·별칭), 프론트 Vercel 환경 변수(NEXT_PUBLIC_API_URL)·front 푸시, Caddy 독립 스택 분리(승인 대기)

## 17:33 — Caddy 독립 스택 분리 완료·구 레포 정리 (서버 에이전트 협업)

- 서버: Caddy를 /opt/edge-proxy 독립 compose로 이관(인증서 볼륨 재사용·재발급 0, Caddyfile 디렉터리 마운트로 inode 함정 제거). 롤백 경로 보존(구 컨테이너 stop 상태)
- prototype-axpoint-api: caddy 서비스·Caddyfile·caddy 볼륨 제거 커밋·푸시(`6aa08a4`) — cron 80/443 충돌 예방(다음 푸시 조건 이행)
- 규약 문서화(`develop`): 서비스명 api는 edge에서도 양쪽을 가리키므로 프록시는 고유 별칭만 사용 — compose 주석·DEPLOY.md 명시
- 사후 점검 전 항목 통과(Resend io.kr verified·FRONT_ORIGIN 다중·별칭 단일 IP·양쪽 200·CORS)
- 남은 것: 사용자 — GitHub에 axcore-dev/edge-proxy 비공개 레포 생성 + 서버 에이전트가 줄 공개키를 Deploy key(Allow write access 체크)로 등록, UptimeRobot에 api.axcore.io.kr/health 등록

## 19:16 — 백엔드 README 작성 + 서버 인프라 완결

- edge-proxy 원격 레포 동기화 완료(1b22ffc, deploy key 쓰기 등록) — 레포 3개 키 분리, 평문 PAT 전량 제거. 구 레포 caddy 제거 커밋은 cron 자동 반영 확인(수동 개입 0)
- api README.md 신규(`develop`): 스택 표·컨테이너/운영 아키텍처 다이어그램·진단 파이프라인·디렉터리·빠른 시작·API 개요·브랜치/배포·협업 규칙 — 신규 개발자 온보딩용
- UptimeRobot 등록 절차 안내(사용자 콘솔 작업)

## 20:50 — 결과·보고서 원본 UI 복구 완료 + 어드민 콘솔 신설

- **결과 화면 원본 UI 복구** (`docs\참고자료\복구가 필요한 UI` 캡처 기준, 서버 데이터 연동 방식):
  - 카테고리별 준비도: 업계 평균 비교 타이틀(±10 3구간)·레이더 평균 점선 폴리곤·범례·호버 2줄 툴팁 복구 — 벤치마크 응답이 있을 때만 렌더
  - 축 카드(모두 보기·선택 상태): ScoreBar 평균 마커·평균 미달 저채도 주황·축별 소견 문장·`점수 근거` 버튼 복구
  - 점수 근거 팝업: 문항 텍스트·판정 기준(앵커 서술)·판정 보류 뱃지·근거 파일명 — 서버 `/result` 응답 보강분 사용
  - AX 진단 결과 상세 카드: 분포 유형 문장·종합 vs 업종 평균 바·전략 유형 Badge+설명 복구
  - 기능영역: 관리 대상 카드 `사유 보기` 버튼 + 인과 체인 팝업(원인→영향 사슬·근거 목록) 복구
  - 미복구 잔여: 공개 데이터 N종 카드·워크플로우 부서 흐름(백엔드 신규 도메인 필요), 통계 칩 상세 팝업·특허 등 5칩(외부 수집 미연동) — `docs\어드민\추후 검토 메뉴.md` 하단에 정리
- **보고서 화면 원본 요약 복구**: 진단일 표기, 요약 4카드(현재 단계·포지션·예상 연 효과·투자 회수), 연 효과 산출 내역 표, 투자 회수 계산식 카드 — `/roadmap` ROI 응답 기반, 값 없으면 "—"/카드 미렌더. 기존 5카드(종합 점수·담은 과제·총 기간·자부담)는 원본 4카드 구성으로 교체(담은 과제 수는 캡션 유지, 기간·자부담은 로드맵 화면·회수 계산식에서 확인)
- **어드민 콘솔 신설** (`/admin`, admin.axcore.io.kr 용):
  - `proxy.ts` 신설 — admin.* 호스트를 `/admin` 하위로 리라이트 (Next 16 proxy 규약)
  - 대시보드·사용자 관리·진단 이력 3탭 — 화면 골격만, API 연동 필요 동작은 "개별 관리 중" 팝업(`components/admin/ManagedModal`)
  - 접근 가드: `/api/me` role=admin 검사(레이아웃), 비로그인·권한 없음 안내 카드. AuthContext에 role 필드 추가, StepBar는 /admin에서 미렌더
  - 제외 메뉴(카탈로그·AI 설정·연동 관리)는 `docs\어드민\추후 검토 메뉴.md`로 문서화, `prototype-axpoint-mrep` 폴더 삭제
- **검증**: 로컬 실서버(front:3000·api:3001·worker) + Chrome으로 캡처 10장 대조. (주)효광 완료 진단을 사용자 계정에 연결해 재열람 경로로 확인. 사유 보기·ROI는 로컬 DB 표시용 백필(생산관리 critical 1건·과제 3건 연 절감액)로 렌더 검증 — 실데이터는 신규 진단(원인 체인 GPT 생성)·xlsx 시드(연 절감액) 반영 시 채워짐
- 계정 참고: 로컬 DB에서 pioh@axcore.ai.kr role=admin 부여(어드민 검증용), 별도 어드민 계정 admin@axcore.ai.kr 생성(비번은 대화로 전달)

## 21:20 — 어드민 사용자 관리 실목록·환경 관리 탭 (지시 정정 반영)

- 정정 반영: "탭만 만들고 팝업" 처리는 환경(키값) 관리에만 해당 — 사용자 관리는 실데이터 표시로 교체
- **사용자 관리**: `GET /api/admin/users` 연동 실목록 — 이메일·이름·회사·로그인 방식(이메일/Google/게스트)·권한(관리자/일반)·상태(인증됨/미인증/체험)·가입일 + 클라이언트 검색. 총 인원 표시
- **환경 관리 탭 신설**: 서비스 환경 변수 **키 이름만** 그룹별(AI·인증메일·외부데이터·인프라) 표시(값은 어디에도 미노출), `관리` 클릭 시 "개별 관리 중" 팝업
- 벤치마크 평균은 백엔드가 스냅샷 로직으로 교체됨에 따라, 업종코드·표본(5건)이 쌓이기 전까지 결과 화면 평균 UI는 표시되지 않음(정의된 동작). 프론트 코드는 조건부 렌더라 변경 없음
- `docs\어드민\추후 검토 메뉴.md` 갱신(사용자 관리 실목록·환경 관리 반영, 연동 상태 모니터링으로 재정리)
