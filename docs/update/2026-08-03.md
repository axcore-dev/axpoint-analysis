# 2026-08-03

## 11:55 — 자료 분류 화면: 양식집(묶음 PDF) 분할 상태 표시

백엔드가 묶음 PDF를 페이지별 파일로 분할 분류하게 되면서(`status='split'` 신설),
`collect` 화면 `STATUS_LABEL`에 `split: "양식집 · 페이지별 분류"` 한 줄 추가.
분할 자식 파일 목록·슬롯 충족·분석 진행률은 기존 서버 응답 필드를 그대로 써서 추가 수정 없음.

**커밋 문구**:
```
feat: 양식집(묶음 PDF) 분할 상태 라벨 추가

- 분류 상태 'split'을 "양식집 · 페이지별 분류"로 표시
```

## 12:38 — 수집 화면·어드민 개선 (승광 테스트 피드백)

- **수집 카드 팝업 원본·출처 탭 통합** — 원본 항목 제목에 원문 링크를 걸고 출처 탭 삭제.
  note·오류 안내는 원본 탭 상단으로. 요약 탭은 그대로.
- **자료 업로드 프로그레스바** — fetch → XHR 전환(`upload.onprogress`), 필수 서류 패널
  헤더 아래에 진행률 바(%) 표시. 100% 후에는 서버 판독 대기.
- **분류 파일 그리드 최대 9개** — 나머지는 '모두 보기 →' 버튼 → 전체 목록 팝업(같은 행 표기 재사용).
- **어드민 프롬프트 화면** — 카드마다 공급자(OpenAI·Claude·Gemini)·모델 드롭다운(선택 즉시 저장),
  자리표시자를 우측 고정 패널로 이동. 선택지는 서버 카탈로그(`providers`)에서 받는다.

**커밋 문구**:
```
feat: 수집 카드 탭 통합·업로드 진행률·파일 목록 팝업·프롬프트 모델 설정 UI

- 카드 팝업 원본·출처 탭 통합 (제목에 원문 링크)
- 자료 업로드 프로그레스바 (XHR 진행률)
- 분류 파일 그리드 최대 9개 + '모두 보기' 팝업
- 어드민 프롬프트: 공급자·모델 드롭다운, 자리표시자 우측 패널
```

## 20:15 — 진단 플로우 개편 1차 (랜딩 원복·자료 확인 단계 신설)

- **랜딩 위저드 4→3단계** (`app/(landing)/page.tsx`) — search → confirm(1/2) → upload(2/2).
  - upload 단계를 필수 서류 슬롯 UI에서 데모 시절 단순 '모두 업로드' 디자인으로 원복:
    중앙 대형 드롭존(업로드 아이콘 + "파일 업로드, 또는 가져다 놓기" + 추천 파일명 칩
    생산일지·발주서·재고표·검사성적서 + 포맷 안내) + 드래그앤드롭 수신, 업로드 파일
    리스트(확장자·삭제 ×), 하단 풀와이드 '다음'. XHR 업로드 진행률 바는 드롭존 아래 유지.
  - AI 분류 관련 제거: required-docs 4초 폴링, '자료를 분석하고 있어요 (n/m)' 게이트,
    자료 부족 경고 팝업, 슬롯별 업로드·부족 슬롯 필터·미분류 드래그앤드롭 전부 삭제.
    업로드 완료만 확인하고 '다음' 활성화(파일 0건이면 '자료 없이 진행').
  - systems 단계(사용 중인 프로그램) 삭제 — collect '자료 확인'으로 이동. DotProgress 3→2.
- **자료 정리 2단계 구성** (`app/(site)/collect/page.tsx`)
  - 1단계 '자료 확인'(신규, 분류 실행 전): 필수 서류 슬롯 패널(랜딩에서 이식 —
    한번에 올리기·부족한 것만·슬롯별 올리기 docTypeId 지정 업로드), 사용 중인 프로그램
    태그(배타 토글·기타 직접 입력, `PATCH {systems}`), 사전 설문(kind=primary) 카드형
    단일 선택 인라인. 하단 '자료가 충분해요' / '자료 없이 진행'(파일 0건) →
    `POST /classify` 호출 후 2단계 전환.
  - 2단계 '자료 분류'(기존 화면): 분류 그리드·공개데이터·워크플로우 유지. 파일 폴링은
    2단계에서만. 보완 설문(kind=supplement) 발행 시 안내 배너 + SurveyModal.
  - 분류가 이미 시작된 진단(분류 행 중 pending 아닌 것 존재)은 2단계 직행.
- **SurveyModal `supplementOnly` prop 추가** (`components/flow/SurveyModal.tsx`) —
  보완 설문만 필터해 표시하고, 첫 판정 전이라 저장만 하고 재판정(submit)은 걸지 않는다.
  결과 화면의 기존 사용처는 동작 변화 없음.

검증: `npx tsc --noEmit` 통과. 커밋은 검수 후 일괄 진행(커밋 금지 지시).

**커밋 문구** (검수 후 사용):
```
feat: 진단 플로우 개편 1차 — 랜딩 업로드 원복·collect 자료 확인 단계 신설

- 랜딩: 슬롯 UI 제거, 중앙 드롭존 원복, systems 단계 삭제 (3-phase)
- collect: 1단계 자료 확인(필수 서류·프로그램·사전 설문·분류 시작 게이트) 추가
- collect: 2단계 자료 분류 유지, 보완 설문 배너 + SurveyModal(supplementOnly)
```

---

## 20:24

### 진단 결과·개선 과제 화면 개편 (백엔드 확장 API 연동)

- **판정 대기 프로그레스바** (`lib/judgeWait.ts`, `app/(site)/result/page.tsx`,
  `components/ui/ProgressBar.tsx` 신설)
  - `waitForJudge`에 `onProgress` 콜백 추가 — 폴링 응답의 `judgeProgress`({judged,total})를
    노출. 기존 호출부(collect·SurveyModal)는 시그니처 그대로 동작.
  - 결과 페이지: `/result` 응답 status가 `judging`이면 3-dot 대신 진행률 바
    (`Math.round(judged/total*100)` + '{percent}%') 표시, 완료 시 결과 재조회.
    실패·타임아웃 문구는 collect의 기존 문구 재사용.
- **스켈레톤 로딩** (`components/ui/Skeleton.tsx` 신설, `components/flow/RouteLoading.tsx` 주석)
  - result 데이터 fetch 동안 전체 3-dot 대신 헤더 블록 + 카드 그리드 골격 스켈레톤.
    셔머 없이 tailwind animate-pulse, `var(--grey-100)` 토큰 사용.
  - RouteLoading 상단 주석 원칙 문구를 '전역 전환은 3-dot, 데이터 로딩은 스켈레톤
    허용(2026-08 개정)'으로 갱신 (동작 불변).
- **기업 헤더 복구** (`app/(site)/result/page.tsx`)
  - `company.dartVerified`면 헤더 우측 '출처 · DART 전자공시 (재무 데이터)' 칩.
  - 통계 칩 스트립을 `publicStats` 기반 확장: 연 매출·고용 유지 + 특허·정부 R&D 과제·
    최근 보도·조달 실적(각 'N건', 0이어도 표시) + 인증(벤처/이노비즈 itemCount>0 라벨,
    없으면 '—'). 가로 스크롤 버튼 패턴 유지.
- **검토 필요 배지** — 점수 근거 팝업에서 `reviewNeeded` 문항에 경고 톤 '검토 필요' 배지 +
  `reviewReason` 문구 + '자료 보완하기' 링크(→ /collect).
- **강등 사유 표시** — `result.capReasons` 있으면 현재 단계 섹션에 '점수 구간은
  Lv.{scoreLevel}이지만 달성 조건 미충족으로 Lv.{level}로 판정됐어요' + 사유 목록.
- **표준 워크플로우 섹션** (`components/flow/WorkflowStandard.tsx` 신설) — 업무영역별 현재
  상태 바로 아래 삽입. GET /workflow의 8대 영역 카드(영역명 / 업무 Task 목록 / 산출 문서
  유형 뱃지 — covered면 accent, 아니면 outline). 가로 스크롤, 카드 min-width 260px.
  기존 `WorkflowSection.tsx`(collect 데모)는 미수정.
- **개선 과제 화면** (`app/(site)/tasks/page.tsx`)
  - TaskRow에 `pinned`/`target`/`services` 반영, 응답 루트 `capReasons` 수신.
  - 정렬: 고정 4카드 → 추천 → 나머지(그룹 내 서버 우선순위 유지). 추천 필터에 pinned 포함.
  - 고정 카드에 image-4 스타일 2행: 파란 '대상'/'서비스' 라벨 + 텍스트, 행 사이 구분선.
  - capReasons 있으면 그리드 상단 경고 톤 배너: '우선 해결 과제' + 사유 목록 +
    '아래 추천 과제부터 담는 것을 권해요'.

검증: `npx tsc --noEmit` 통과. 커밋 금지 지시로 커밋하지 않음.
주의: 랜딩·collect 페이지의 워킹트리 변경분은 다른 에이전트 작업분 — 본 작업에서 미수정.

**커밋 문구** (검수 후 사용):
```
feat: 진단 결과·개선 과제 화면 개편 — 판정 진행률·스켈레톤·공개데이터 칩·표준 워크플로우

- judgeWait onProgress 노출, result 판정 중 프로그레스바(ProgressBar 신설)
- result 로딩 스켈레톤(Skeleton 신설), DART 출처 칩·publicStats 통계 칩 확장
- 검토 필요 배지·강등 사유 표시, WorkflowStandard 섹션 신설
- tasks: pinned 4카드 고정 + 대상·서비스 2행, capReasons 배너
```

---

## 20:30

### collect 2단계 마저 개편 — 워크플로우 실데이터 활성화 + 분류 진행 로그

- **워크플로우 실데이터 전환** (`components/flow/WorkflowSection.tsx` 전면 재작성,
  `components/flow/WorkflowStandard.tsx`에서 `WorkflowStage`·`OutputDoc` 타입과 `stageDocs` export)
  - 하드코딩 데모 상수(STANDARD/DEMO_ORDER/DEMO_STAGES/SUPPORT) 삭제. GET
    /api/assessments/:id/workflow를 스스로 조회해 8대 영역 카드 렌더 (WorkflowStandard와 같은 데이터).
  - '데모 · 개발 중' 뱃지와 예시 데이터 안내문 제거. 아코디언 유지하되 기본 펼침(open 초기 true).
  - 카드 3행: 영역명(+표준 대비 '{n}/{m} 보유') / 업무 Task 목록 / 산출 문서 유형 뱃지
    (covered=accent, 미보유=outline). min-width 260px + overflow-x-auto 가로 스크롤.
  - {companyName} 칩 + '표준 대비 보유 문서' 라인으로 표준 vs 이 기업 비교 취지 유지.
  - 표준 정의를 못 받거나 stages가 비면 섹션 자체를 숨긴다 (WorkflowStandard와 동일 정책).
- **분류 진행 로그** (`components/flow/ClassifyProgress.tsx` 신설, collect 2단계 파일 그리드 상단)
  - collect가 폴링 중인 files를 props로 받아 완료 → 진행 중 → 대기 순 텍스트 리스트 렌더.
    done: 체크 + '{파일명} — {소분류명} · L{레벨}' / processing: 3-dot 미니 로더 + '{파일명} 분류 중' /
    pending: 옅은 '{파일명} 대기' / failed·unclassified·split: 그리드 STATUS_LABEL과 같은 문구.
  - 완료 행이 늘면 진행 중인 행이 하단에 보이게 프로그램 스크롤(scrollTo smooth). 컨테이너
    overflow: hidden으로 휠 스크롤 차단, maxHeight 180px + 위쪽 페이드 마스크(mask-image).
  - 분류 미완료 파일이 있는 동안(classifying)만 표시, 전부 완료되면 숨김.
- **collect/page.tsx**: WorkflowSection에 assessmentId 전달, ClassifyProgress 배치. 기존 문구 변경 없음
  (데모 뱃지·안내문 제거는 지시 범위).

검증: `npx tsc --noEmit` 통과. 커밋은 지시대로 하지 않음(커밋 금지 지시).

**커밋 문구** (검토 후 사용):
```
feat: collect 워크플로우 실데이터 활성화 + 분류 진행 로그 애니메이션

- WorkflowSection: 데모 상수 제거, /workflow 실데이터 카드(영역/Task/산출문서, {n}/{m} 보유)
- WorkflowStandard: WorkflowStage·OutputDoc·stageDocs export (재사용)
- ClassifyProgress 신설: 분류 과정 텍스트 로그, 자동 스크롤 + 페이드 마스크
```

---

## 20:42

### 자료 정리 2단계 '자료 편집' 칸반 팝업 (사용자 인터뷰 확정안)

- **FileEditBoard 신설** (`components/flow/FileEditBoard.tsx`) — 풀스크린에 가까운 대형
  모달 안 가로 스크롤 칸반 보드.
  - 컬럼: 8대 기능 영역(doc_group, GET /api/doc-groups) + 'ISO·표준경영'(ISO 4그룹 묶음
    1컬럼) + '미분류'. 파일 배치는 docTypeId → 그룹 매핑, 양식집(split) 부모는 제외.
  - 카드: 파일명 + 소분류명 + L레벨 뱃지 + 분류 상태(대기/분류 중 로더). HTML5
    드래그앤드롭(라이브러리 없음)으로 컬럼 간 이동, 이동 시 '변경됨' 점 표시.
    ISO 컬럼 카드에는 관리영역 4개 선택 미니 드롭다운(기존 ISO 그룹이면 유지, 아니면 첫 그룹).
  - 컬럼 하단 '+ 파일 추가' — 선택 즉시 업로드(POST files) 후 해당 컬럼 배치(변경 취급).
    미분류 컬럼은 영역 지정 없이 업로드만.
  - [취소] [저장] — 저장 시 변경 파일마다 `PATCH /api/files/:id/group {groupId}`
    (미분류 이동 건은 PATCH 제외), 마지막에 `POST /classify {fileIds: 변경 전체}` 1회.
    닫으면 부모가 목록 새로고침, 멈춰 있던 3초 폴링 재개로 재분류 진행을 이어받는다.
  - 안내 카피: '파일을 끌어 영역을 바꾸면 그 영역 안에서 AI가 다시 분류해요'.
- **Modal `full` prop 추가** (`components/ui/Modal.tsx`, `app/globals.css`
  `.ax-modal--full`) — 100vw−40px(최대 1560px)·100vh−40px 대형 변형. 기존 wide 동작 불변.
- **collect 연결** (`app/(site)/collect/page.tsx`) — 2단계 파일 그리드 헤더에
  '자료 편집' 버튼(우측 정렬), FileRow에 `docTypeId` 필드 추가(서버 응답에 이미 있던 값).
  기존 문구 변경 없음.

검증: front `npx tsc --noEmit` 통과, api `npm run typecheck` 통과(GET /api/doc-groups
신설 — api 기록은 메인 세션이 일괄 작성). 커밋 금지 지시로 커밋하지 않음.

**커밋 문구** (검수 후 사용):
```
feat: 자료 정리 2단계 자료 편집 칸반 팝업 — 영역 드래그앤드롭·추가 업로드·부분 재분류

- FileEditBoard 신설: 8대 영역 + ISO·표준경영 + 미분류 컬럼, HTML5 드래그앤드롭
- 저장 시 변경 파일만 PATCH group 후 classify {fileIds} 1회 (변경 없는 파일 재분류 안 함)
- Modal full 변형(.ax-modal--full) 추가, collect 그리드 헤더 '자료 편집' 버튼 연결
```
