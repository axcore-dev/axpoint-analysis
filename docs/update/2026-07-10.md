# 2026-07-10 작업 기록

## 12:57 — S0 랜딩(app/page.tsx) 수정요청v2 반영

- 검색 화면: 검색바를 뷰포트(스텝 헤더 제외 영역) 수직 중앙에 배치, 헤드라인·서브는 검색바 위에 부착. 헤드라인 `.ax-heading` + 핵심 구절 `<b>` 강약 적용("3분이면 나와요, **우리 회사의 AI 도입 답안지**").
- 예상 검색어: 검색창 포커스/입력 시 "데모기업", "AXCRE" 2건 드롭다운(흰 카드+헤어라인+shadow-2, 호버 워시). 입력값 필터링(매칭 없으면 둘 다 표시), 클릭·↑↓/Enter/Escape 지원, 선택 시 입력 채우고 로그인 체크 후 confirm 진행.
- 2/3 자료 올리기: 참고 UI(파일 업로드.png) 문법 반영 — 점선 업로드 존(아이콘 타일 + "눌러서 올리거나, 끌어다 놓아 주세요" + 포맷 안내 + "파일 선택" 필 버튼) 아래에 파일 리스트가 행으로 누적(파일 아이콘 + 파일명 + 타입·더미 용량 + "분석 준비 완료" success Badge). 업로드 존은 업로드 후에도 유지되어 추가 업로드 가능(클릭당 4건 × 3회, 이후 "더 올릴 자료가 없어요" 안내). 제목 강약: "**현장 서류**를 올려주세요". DiagnosisContext에는 uploadSimulated(1건 이상 여부)만 반영.
- 버튼 활성 규칙: 2/3 [다음]은 1건 이상 업로드 시 활성(미업로드 시 ghost [자료 없이 진행]). 3/3 [진단 시작하기]는 시스템·관심 영역 1개 이상 선택 시 활성, 미선택 시 disabled + "하나 이상 고르시면 시작할 수 있어요" 캡션, ghost [건너뛰고 시작] 유지.
- 수정 범위 JSX를 인라인 스타일 → Tailwind 유틸리티(bg-surface, text-ink-*, border-line 등)로 전환. 동적 값·컴포넌트 오버라이드만 인라인 유지.
- `npx tsc --noEmit` 오류 0 확인.

## 13:04 — S1 자료 정리(app/collect/page.tsx) 수정요청v2 재구성

- 상단 내부 2단계 표시 도입: [1 자료 확인] → [2 자료 분류] (화살표 구분, 선택 배경색 없이 블루 텍스트·번호 강조, 완료 체크 표시). 최초 진입 수집 로딩은 공용 `RouteLoading`으로 교체(기업명 title + 순환 메시지 4종, 재방문 시 생략).
- 단계 1 — 자료 확인(모달 체인): 안내 카드(`.ax-heading` 강약 "<b>딱 맞는 결과</b>를 위해…" + "확인 2건과 짧은 설문이 있어요" + [시작하기]) → 모달 1 자료 검증(d07·d08 순차, 진행 1/2·2/2, 질문→출처 작게→선택지, 응답 시 자동 진행, "나중에 다시 바꿀 수 있어요" 삭제) → 모달 2 설문(1번·10번 제외 = 11문항, 한 문항씩 n/11 + 이전/다음, 건너뛰기 삭제, 기타/복수/사유 입력 유지, "기본 응답으로 채우기" 상단 유지, 1번 제외 캡션 "사용 중인 시스템은 자료 올리기에서 알려주셨어요").
- 체인 완료 → surveyDone·confirmedDocIds 반영 → RouteLoading 1.6초("응답을 판정에 반영하고 있어요" → "자료를 8대 영역으로 나누고 있어요") → 단계 2 전환. 재방문(확인 완료) 시 바로 자료 분류 표시, 단계 1 탭 클릭 시 완료 요약 + [다시 열기].
- 단계 2 — 자료 분류: 헤더 "총 <b>48건</b>을 수집했어요"(업로드 12 + 공개 36, 런타임 계산·모노 숫자). 공개 데이터는 건수 많은 순 2~3열 콤팩트 그리드 — sourceApi 서브텍스트 삭제, 우측 "출처" 텍스트 버튼 → 출처 팝업(API 단어 제거 표기 + 한 줄 설명), 행 클릭 → 원문 요약(items) 팝업.
- 8대 영역: 아코디언 → 가로 탭 전면 교체(탭별 자료 수). 탭 내부는 L 낮은 순 정렬 + L별 그룹 라벨(L1 수기 → L4 시스템출력). 뱃지 배경 컬러 제거(개인정보 가림·자료 부족 뱃지 삭제, L·문서유형은 배경 없는 텍스트), 파일명·제목 font-light. 부족 데이터(design·cs)는 warning 컬러 Light 제목("도면 관리 대장이 아직 없어요" 식) + missingHint 설명 + [업로드] secondary sm(1초 로딩 → "받았어요 — 다음 진단에 반영할게요"). extractedDetail 상세는 콤팩트 접기 유지.
- [진단 결과 보기] 하단 중앙 플로팅(fixed, shadow-2), 확인 완료 시 활성 → completeStep("collect") → /result. 가드(companyInput 없음) 유지.
- 작성 JSX는 Tailwind 유틸리티 우선(bg-surface/text-ink-*/border-line/[font:var(--text-*)] 등), 공유 파일 수정·신규 패키지 없음.
- `npx tsc --noEmit` 오류 0, `next build` 통과(11페이지 정적 생성). eslint `react-hooks/set-state-in-effect` 3건은 하이드레이션 패턴에서 발생 — app/page.tsx·report·result 등 기존 페이지와 동일한 프로젝트 공통 패턴.

## 13:14 — 수정요청v2 통합 검증 완료

### 공통 (직접 작업)
- 스텝 헤더: 선택 단계 배경 컬러 제거(블루 텍스트·번호 배지로만 강조)
- 푸터: 회사 정보로 교체((주)에이엑스코어·대표·사업자번호·본사·R&D 센터·이메일·© 2026)
- Tailwind 지향: globals.css에 @theme inline 토큰 매핑(bg-surface/text-ink/border-line 등) — 이후 작성·수정 코드는 Tailwind 유틸리티 우선(v2 규칙, 디자인 README에 명문화)
- 라우팅 로딩 규칙: components/flow/RouteLoading 공용화(3-dot + 해요체 진행형 문구, 320ms ease-out)
- 제목 굵기 강약: .ax-heading(기본 400 + <b> 700) 규칙 추가
- 죽은 코드 제거: components/flow/GlobalNav.tsx (v2 헤더 삭제 후 미사용)

### SOLID / CSS 하드코딩 점검 결과
- 인라인 스타일 최다: app/result/page.tsx(162곳·1,599줄 — 단일 파일에 5개 섹션+차트+모달, SRP 위반 최대 지점), report/roadmap/tasks 각 49~60곳
- components/report/ReportDocument.tsx(114곳)는 html2canvas 제약(oklch 미지원)에 따른 의도적 인라인 — 예외 유지
- 이번 범위(S0·S1·공통)는 Tailwind로 전환 완료. S2~S5 인라인 정리 및 result 페이지 섹션 분리는 후속 리팩토링 항목으로 남김

### S0 자료 올리기 (서브에이전트)
- 검색바 수직 중앙 앵커 + 헤드라인 강약, 예상 검색어 드롭다운(데모기업/AXCRE, 키보드 지원)
- 2/3: 참고 UI(파일 업로드.png) 반영 — 업로드 존 유지 + 파일 리스트 누적(4건씩 3회, 타입·용량·"분석 준비 완료" 배지), 워딩 교체("눌러서 올리거나, 끌어다 놓아 주세요")
- 활성 규칙: 2/3 [다음] = 1건 이상 업로드 시, 3/3 [진단 시작하기] = 1개 이상 선택 시(캡션 안내) — 건너뛰기는 ghost 경로 유지

### S1 자료 정리 (서브에이전트)
- 내부 2단계 [1 자료 확인] → [2 자료 분류] + 전환 RouteLoading
- 자료 확인: 안내 카드("딱 맞는 결과…" 강약) → 모달 체인(자료 검증 2건 → 설문 11문항) — "나중에 바꿀 수 있어요" 삭제, 설문 1번(시스템, S0 중복) 제외+캡션, 건너뛰기 버튼 삭제
- 자료 분류: "총 48건을 수집했어요"(런타임 합산), 공개 데이터 콤팩트 그리드(건수 내림차순·서브텍스트 삭제·[출처] 팝업(API 표기 제거)·행 클릭 원문 팝업), 8대 영역 Tabs(L 낮은순 그룹, 뱃지 배경 제거, Light 제목, 부족 데이터 warning 컬러 제목+설명+[업로드] 버튼), [진단 결과 보기] 플로팅
- 데이터 무변경 — 설문 1번은 화면에서만 제외(판정 근거는 S0 시스템 응답으로 간주, 점수 57·Lv.3 유지)

### 검증
- `tsc --noEmit` 오류 0, `npm run build` 성공(11 페이지)
- 브라우저 E2E: 자동완성 선택 → 로그인 팝업 → 3단계 위저드(활성 규칙·추가 업로드 확인) → S1 확인 모달 체인 → 전환 로딩 → 자료 분류(출처 팝업·제품설계 탭 부족 데이터) → 플로팅 CTA·새 푸터 — 전 구간 정상, 콘솔 에러 0건

## 17:42 — 수정요청v3 전면 반영 + AX 7단계 방법론 정합

### 데이터 레이어 — 7단계 방법론 정합 (docs/참고자료/중소 제조기업 AX 7단계 방법론.xlsx)
- `data/catalog/method.ts` 신설: 7단계(경영문제 정의 → 데이터화·표준화 → 생산 모니터링 → 품질 안정화 → 재고·물류 → 설비 → 공정 최적화·확산) 정의 SSOT — 단계별 목적 문구 포함
- 과제 카탈로그: 전 과제에 `methodStep`(2~7) 부여, 영문 subtitle 필드 삭제. 중복 정리 — t20 수기 장부 자동 판독(t03 모바일 작업일보와 중복) 제외, t07은 "설비 이상징후 감지(예지보전)"로 역할 분리(수집은 t11, dependsOn t11), t21 "공정조건 최적화·라인 확산" 신설(7단계 완결). 총 20종 유지
- `coveredBySystems` 신설: 사용 중인 프로그램(MES→t03·t06, WMS→t04·t18, ERP→t08)으로 이미 갖춰진 과제는 선택 불가 처리
- `lib/roadmap.ts` 재작성: 로드맵 단계 축 = 방법론 단계. 1단계 경영문제 정의는 "이번 진단으로 완료"(done), 2~7단계는 methodStep 그룹(빈 단계 제거). 게이트(진행 기준) 폐지, 귀사/AXpoint 역할 → 단계별 `todos` 통합 리스트, goalLine 동적("N개월 뒤, 한 번 입력한 데이터가 스스로 흐르는 현장"), 비용 note 축약. rubric/scenario 무변경 — 종합 57점·Lv.3 유지 확인

### 공통
- 새로고침 초기화: DiagnosisContext sessionStorage 유지 제거(새로고침 시 데이터 초기화, 라우트 이동은 유지) + 작업 진행 중 beforeunload 브라우저 경고
- SiteFooter 신설(footer레이아웃만 참고.png — grey-900 다크, 로고 블록+4컬럼+카피라이트 행): 첫 페이지(/)·보고서(/report)에만 노출, 기존 공통 푸터 제거
- 툴팁 소형화: `.ax-tooltip` 공용 클래스(밝은 서피스+어두운 텍스트, 캡션급, fade-in) — TermTooltip·레이더 툴팁 반영. `--orange-muted`(저채도 주황)·`.ax-scrollbar-none` 토큰/유틸 추가
- 라이팅 규칙: 행동형 문체는 행동 흐름 적재적소에만, 정보 표시는 직관·간결 — CLAUDE.md에 "사용자 수정 문구 되돌리기 금지 + 라이팅 기본값" 규칙 추가

### S0 자료 올리기
- 데모 상수(SUGGESTIONS·UPLOAD_BATCH_SIZE·dummySize) 삭제 → `components/ui/Autocomplete.tsx` 신설(WAI-ARIA 콤보박스: 필터링·방향키·Enter·Esc·매칭 하이라이트·설명 병기, 무의존성)
- 업로드: 1회 일괄 첨부로 단순화. 파일 리스트 콤팩트(아이콘·뱃지·더미 용량 삭제, 파일명+타입만, label-s)
- 3/3: "하나 이상 고르시면…" 서브텍스트·"건너뛰고 시작" 삭제, 프로그램+관심 영역 모두 선택해야 활성, '없음' 배타 선택(선택 시 나머지 해제), 제목 "사용 중인 프로그램과 관심 영역을 골라주세요"
- BackButton·Progress → `components/ui/CardNav.tsx`(BackIconButton·DotProgress)로 공용화

### S1 자료 정리
- FlowStepper 신설(components/ui/Stepper.tsx — 참고 코드 circle variant를 디자인 시스템 문법으로): 자료 확인 → 자료 분류 가운데 정렬, 완료 시 체크
- 설문 팝업 폐지: 자료 올리기와 동일한 카드 위저드로 통합(자료확인 → 설문 2개 흐름), 선택지는 체크+행(CheckRow) UI, "기본 응답으로 채우기" 유지
- "확인을 모두 마쳤어요" 카드: 체크 아이콘 원형 배경(success-weak), 다시 열기 → 좌상단 뒤로가기, 자료 분류 보기 → secondary 배경+→
- 공개 데이터: 행의 출처 버튼 삭제 → 카드 팝업 탭(정보/출처)으로 통합, 팝업 크기 통일(minHeight)
- 8대 영역: 자료 부족 블록·업로드 시뮬레이션 삭제("받았어요 — 다음 진단에 반영할게요" 카피 제거), 탭 스크롤바 숨김
- 진단 결과 버튼: 플로팅 → 콘텐츠 우측 하단 배치 + 화살표

### S2 진단 결과
- 섹션 1·2·종합 소견 3카드 → 단일 통합 카드(기업 식별 → "현재 Lv.3 단계예요" 메인 카피 + 업종 평균 비교 서브 + DotStepper → 연한 구분선 → 통계 칩(min-width 통일) → "첫 단추는 품목 코드 표준화와 모바일 작업일보" 한 줄). '한마디로' 섹션 삭제
- 레이더: 호버 툴팁 소형·밝은 서피스·평균 병기, 축 텍스트 호버 강조, 선택 축 radialGradient(선택 꼭짓점 블루 → 회색, 전환 페이드), 도트·라벨 부드러운 트랜지션
- 우측 카드: 모두 보기 버튼 카드 밖 우측, 요약으로 → 좌상단 뒤로가기 화살표, 평균 미달 프로그레스바 저채도 주황, 점수 근거 팝업 스크롤 내부화
- 기능영역: 개선과제 버튼(카드·팝업)·자료 추가하기 삭제, 판단 보류 "자료를 올리면…" 문구 제거(areas.ts)
- 가치사슬+종합 분석 통합: 가치사슬은 아코디언(디폴트 접힘, #태그 3종만 노출, 펼치면 재고 흐름 표 — 용어 툴팁·연결 칩·신호 카드 삭제), 종합 분석은 보고서형 단일 카드(강점/보완/AX 전략 제안 불릿), CTA 우측 하단+화살표

### S3 개선 과제
- 카테고리 칩: ★추천(기본 선택) 맨 앞 + 전체 + 방법론 단계 6종(카운트). 영역 필터·?area= 폐지
- 카드: 영문 서브타이틀·즉시 착수·기반 과제 뱃지 삭제(카테고리=단계+★추천만), 기대효과(beforeAfter)는 추천만, 이미 갖춰진 과제는 반투명+선택 불가("{시스템} 사용 중 — 이미 갖춰져 있어요")
- 추천 과제 기본 담기 제거(진입 시 0개)
- 하단 바: grey-800 → 밝은 서피스+그림자 구분, "정부 지원사업 1회 신청 단위: 2~3개", 초과 경고 "우선순위 높은 2~3개를 권해요"로 축약. 연관 제안 토스트도 밝은 서피스

### S4 로드맵
- 헤더: 숫자만 브랜드 컬러, "예상 자부담" → "금액", goalLine 군더더기 제거(동적 N개월)
- 단계 = 방법론: 단계 1 경영문제 정의 "완료" 카드(진단으로 마침), 이후 단계는 목적 문구+과제(기간·금액 데이터 병기)+금액+할 일 통합 리스트(귀사/AXpoint 태그)
- 카드 좌측 띠 제거, "다음 단계 진행 기준" 게이트 카드 삭제, 레일 마커 "1~2개월차" → "약 N개월"
- 스크롤 중앙 포커스: IntersectionObserver(중앙 ±24% 밴드) — 중앙 카드 선명, 나머지 opacity 0.55, 부드러운 전환. CTA 우측 하단

### S5 보고서
- 현재 단계 카드: "정형화·부분 시스템 — …" 서술 삭제(단계 라벨만, PDF 표지 동일)
- 강조 컬러 정리: 수치 강조는 잉크(fg-primary) 볼드로, 브랜드 블루는 인터랙션에만
- 보고서 받기 ↔ 문의하기 버튼 컬러 교체(문의하기 = primary)
- PDF(ReportDocument): 로드맵 섹션 방법론 단계 반영(완료/약 N개월·금액·할 일 통합), 기반 과제 표기 삭제

### 검증
- `tsc --noEmit` 0건, `npm run build` 성공(11페이지 정적 생성). eslint `react-hooks/set-state-in-effect`는 기존 하이드레이션 패턴(미수정 파일 포함) — 이번 범위 외
- 브라우저 E2E(dev): 자동완성(하이라이트·설명) → 로그인 → 3단계 위저드('없음' 배타·일괄 업로드·게이팅) → FlowStepper·카드 위저드(문서 2건 자동 진행 → 설문 체크행 → 기본 응답) → 자료 분류(팝업 탭·8영역) → 진단 결과(통합 카드·레이더 그라데이션·주황 바·아코디언 #태그·보고서 카드) → 개선 과제(방법론 칩·0개 담김·연관 제안 토스트) → 로드맵(완료 단계·약 N개월·금액·중앙 포커스) → 보고서(버튼 컬러 교체·단계 라벨) — 전 구간 정상, 콘솔 에러 0건
- 미결: 사용자가 이전에 직접 수정한 문구 목록은 git에 없어 복원 불가 — 이번 카피 정리에 통합, 이후 사용자가 고치는 버전이 최종본(CLAUDE.md 규칙으로 보호)

## 18:07 — UI 컴포넌트 라이브러리 도입 (사용자 예외 승인)

자체 구현했던 UI 동작 로직을 검증된 라이브러리로 교체 (사용자가 UI 패키지 추가를 예외적으로 허용). 시각(디자인 시스템 토큰·클래스)은 전부 기존 그대로, 동작 레이어만 교체.

### 추가 패키지 (6종) 및 도입 전 검증 결과 (CLAUDE.md 의존성 규칙 준수)

| 패키지 | 버전 | 주간 다운로드* | 최근 배포 | 라이선스 | 용도 |
|---|---|---|---|---|---|
| `downshift` | 9.4.0 | 약 360만 | 2026-06-30 | MIT | Autocomplete 콤보박스 로직(필터·방향키·선택·ARIA) |
| `@radix-ui/react-dialog` | 1.1.19 | 약 6,400만 | 2026-07-06 | MIT | 공용 Modal (포커스 트랩·스크롤 잠금·ESC 내장) |
| `@radix-ui/react-tooltip` | 1.2.12 | 약 5,400만 | 2026-07-06 | MIT | TermTooltip (포지셔닝·지연·충돌 회피 내장) |
| `@radix-ui/react-tabs` | 1.1.17 | 약 5,500만 | 2026-07-06 | MIT | 8대 영역 탭 + 공개 데이터 팝업(정보/출처) 탭 |
| `@radix-ui/react-collapsible` | 1.1.16 | 약 5,100만 | 2026-07-06 | MIT | 가치사슬 신호 아코디언 |
| `react-intersection-observer` | 10.0.3 | 약 440만 | 2026-02-18 | MIT | 로드맵 스크롤 중앙 포커스(useInView) |

\* npm registry 다운로드 API(last-week) 기준, 2026-07-10 조회.

- 유지보수: 6종 모두 최근 5개월 내 배포 이력 확인(Radix 4종은 4일 전 일괄 배포) — 활성 유지보수 판정
- 타이포스쿼팅: 정확한 공식 표기 확인 — `@radix-ui`는 Radix 공식 스코프, `downshift`(downshift-js 팀), `react-intersection-observer`(thebuilder) 원본 패키지명과 일치
- 보안: 설치 후 `npm audit` — 신규 패키지 기인 취약점 0건 (moderate 2건은 기존 `next`→`postcss` 의존성으로 이번 설치와 무관)
- React 19 / Next 16 호환: peer dependency 충돌 없이 설치, 빌드·E2E 정상

세부 반영:
- Modal은 기존 .ax-scrim/.ax-modal 스타일·props API 유지 → 사용처(7곳) 수정 없음
- TermTooltip은 .ax-tooltip 스타일 유지, z-index 120 추가(모달 내부 대응)
- Tabs는 `data-[state=active]:` Tailwind 셀렉터로 기존 룩 유지
- FocusRow의 수동 IntersectionObserver 코드는 useInView로 대체·삭제

### 버그 수정
- downshift 전환 중 발견: blur 시 `selectedItem: null` 기준으로 입력값이 초기화돼 기업명이 지워지는 문제 → `stateReducer`로 InputBlur 시 입력값 유지

### 검증
- `npm run build` 통과(11페이지). 브라우저 E2E 재수행: 자동완성(선택 후 기업명 유지 확인) → Radix 로그인 모달 → 위저드 → 자료 분류 Radix Tabs 전환·팝업 탭 → 진단 결과 Collapsible 펼침(재고 흐름 표) → 로드맵 useInView 중앙 포커스 — 전 구간 정상
- 부수 확인: beforeunload 이탈 경고가 실제 동작(자료 정리 중 이탈 시 브라우저 다이얼로그)

## 18:35 — 랜딩 업로드 존 개선 + 설문 클릭 자동 진행

### S0 자료 올리기 — 업로드 존 재정리 (중복 제거·서류 종류 강조·파일 삭제)
- 중복 제거: 업로드 존 안의 "파일 선택" 버튼 삭제(존 전체가 이미 클릭 영역 — 안내 문구·아이콘과 역할 중복), 서류 안내 문장("생산일지·발주서·재고표 같은 현장 서류면 충분해요")도 칩으로 대체
- 어떤 서류를 올려야 하는지 강조: [생산일지] [발주서] [재고표] [검사성적서] 칩 4종을 업로드 존 중앙에 배치(흰 배경+테두리로 회색 존 위에서 도드라짐). 메인 카피("현장 서류를 올려주세요" + "자료를 올릴수록…")는 유지
- 파일 삭제: 첨부 상태를 파일 단위(attachedIds)로 전환 — 리스트 각 행 우측에 X 버튼, 삭제 시 즉시 제거·전부 지우면 업로드 전 상태(다음 비활성)로 복귀, 재클릭 시 지운 파일 포함 재첨부. 파일 리스트 스크롤바 숨김(.ax-scrollbar-none)

### S1 설문 — 클릭 시 자동 진행 (다음 버튼 제거)
- 단일 선택 문항: 선택지 클릭 → 400ms 후 자동으로 다음 문항(문서 확인과 동일 리듬). 연타 중복 진행은 타이머 가드로 방지
- 다음/완료 버튼은 진행 수단이 따로 필요한 경우에만 노출: 복수 선택 문항(모두 골라 주세요), 입력란이 열리는 선택지(기타·투자 내역 '있다'·'불가' 사유)
- "기본 응답으로 채우기"는 유지

### 검증
- `tsc`·`npm run build` 통과. 브라우저: 업로드 존 칩 표시·파일 X 삭제(3월 생산일지 제거 확인)·복원 동작, 설문 2/11 단일 문항 클릭 → 3/11 자동 진행(버튼 없음)·multi 문항은 다음 버튼 노출 — 정상
