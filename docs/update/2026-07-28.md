> 시각은 해당 작업의 api 저장소 커밋 타임스탬프 기준 (2026-07-28 교정 — 이전 추정 시각 오류 수정)

## 09:58 — 작업 요청v2: DB 스키마 확정 + 인터뷰(B0) 완료 + B1 착수

- **DB 전체 스키마 SQL 작성**: `axpoint-analysis-api\docs\DB(new)\schema.sql` — 인증 4테이블(better-auth 미러) + 마스터 11테이블(축·문항·앵커·문서분류·설문·성숙도·과제·의존성) + 트랜잭션 11테이블(기업·진단·파일·분류·판정·점수·결과·리드) + 인덱스·트리거
- **인터뷰(B0) 완료** — 확정 사항은 `백엔드 개발 계획.md` §6 표로 갱신:
  - 총 47문항(CU-08 결번 유지), 문항당 판정 1개(앵커 점수 그대로), 종합 = 문항 총평균
  - API는 Hono 독립 서버(`axpoint-analysis-api`) — 계획서의 Next.js Route Handlers 결정을 개정
  - 인증 이메일+구글 OAuth / HWP 후순위 / 스캔본 LLM vision 시작(OCR 벤더 보류) / 구 DB 백업만 / 프론트는 백엔드 선행 후 순차 연동 / 탈퇴 시 이력 CASCADE 삭제
- **QnA 문서 신규**: `docs\작업 지시\QnA.md` — 스키마 유지 답변, 업로드 리밋(50개/50MB)·큐 병렬 제안, AI 자동화 설계 제안 6종(규칙 엔진·무설문 엔진·워크플로우·기능영역·추천 로직·보고서), 데모 계정(`demo@axcore.ai.kr`, 비밀번호는 env 주입)
- **B1 착수** — `axpoint-analysis-api` 프로젝트 스캐폴드:
  - git 초기화, main(배포)/develop(작업) 브랜치 분리, 커밋 2건 완료
  - Drizzle 스키마(`src/db/schema/{auth,master,transaction}.ts`) = schema.sql 미러, `tsc --noEmit`·`drizzle-kit generate` 통과, 마이그레이션 산출(확장·updated_at 트리거 포함)
  - Hono 엔트리(`/health`), docker-compose.dev.yml(pgvector pg16+redis+minio), `.env.example`(형식만), `reference-forms/` 폴더(방법론 C 양식 저장소 — 여기에 양식 파일 넣으면 됨)
- 다음: 시드 추출 스크립트(`AX 프로파일링.xlsx` → JSON → upsert), 로컬 Docker로 마이그레이션 실행 검증

### 커밋 문구 제안 (front 저장소 — 문서 변경분)
```
docs: 백엔드 계획 인터뷰 확정 반영 + QnA 답변 문서 추가
```

## 09:59 — 피드백 반영: 주석 구체화

- 코드 주석의 단계 코드(B3·B10 등) 참조 제거 — 처음 보는 사람도 이해할 수 있게 구체 설명으로 교체
- `.env.example` 변수별 설명 보강 (용도·발급처·로컬/운영 구분)
- api 저장소 develop 브랜치에 커밋 완료 (`d294cac`)

## 11:36 — DB(new) 쉬운 설명 문서 추가

- `axpoint-analysis-api\docs\DB(new)\README.md` 신규 — 4개 영역(인증/마스터/트랜잭션/기타) 구분, 진단 1회의 테이블 흐름(mermaid), 마스터 연결 구조, 설계 결정 5가지(스냅샷·결측·CASCADE 등), FAQ
- api 저장소 develop 브랜치 커밋 (`3d8c03a`)

## 11:43 — DB 설계 문서 3종 표 형식 개선 (예시 이미지 기준)

- `01-개념적 설계.md` — **개체 속성 추출** 표(개체/속성/키 속성/비고) + **관계 추출** 표(관계/참여 개체/관계 유형/비고) 형식 적용. 인터뷰 확정(47문항, 구 테이블 폐기 확정) 반영 (`266fd3c`)
- `02-논리적 설계.md` — 릴레이션 스키마 표 형식(속성 헤더 + PK/FK 표기, 유일 제약 주석) 적용 (`244b636`)
- `03-물리적 설계.md` — 테이블별 컬럼 상세 명세표(국문명/영문명/데이터 타입/NN/기본값/PK/FK/제약조건) 26개 테이블 전체 작성. 시드·이관 항목도 인터뷰 확정(47문항, 이관 없음)으로 갱신 (`244b636`)

## 11:48 — 설계 점검 피드백: 사용자 프로필 누락 보완

- 마이페이지 "내 정보"의 회사명·직책·연락처가 DB 설계에 누락됨을 확인 (프론트 `AuthContext`의 company/title/phone 선택 필드)
- `user` 테이블에 3개 컬럼 추가 — better-auth additionalFields 확장 방식. schema.sql·Drizzle 스키마·설계 문서 3종 동기화, 마이그레이션 재생성 (`276fc17`)

## 12:41 — DB 전수 점검 (프론트 전체 ↔ 스키마 대조)

- 서브에이전트 2개로 프론트 전수 조사(데이터 모델·화면별 흐름) + 기능명세서 재검토 + 구 DB 대조 + schema.sql↔Drizzle 정합성 자동 검증(테이블·컬럼 일치 확인)
- 사용자 승인 절차를 거쳐 반영 확정:
  - **A그룹(필수 5건)**: user.role(HITL·컨설턴트), user_consent 테이블(약관·개인정보·마케팅 동의 이력 — 프론트에 전무했음), assessment.completed_steps(이어하기)·rubric_version(판정 기준 버전), lead.assessment_id (`e704dfc`)
  - **B그룹(화면 근거)**: area_judgment 테이블(8대 기능영역 판정 — 관리대상/보통/강점/보류 + As-Is + 인과체인), 설문 자유입력(survey_answer.detail)·노출규칙(survey_question.meta) (`5221094`)
  - **C그룹(선택 반영)**: 과제 부가정보 3칸(시스템대체·데이터요건·효과가정 — 난이도는 제외 확정), 기업 프로필 3칸(업종코드·종업원수·매출) (`5221094`)
- **보류 항목은 `docs\작업 지시\미결정 사항.md` 신설**해 일원화 — 업종 평균 벤치마크(배치 계산+스냅샷 방법 제안 첨부, 상세 인터뷰 예정), 개인정보 마스킹, 로드맵 단계 문구 시드화, 가치사슬 저장 칸, 워크플로우 저장, 공개 데이터 범위, 추천 저장 여부 등 16건
- 최종: 테이블 28개, 타입체크·마이그레이션 생성·정합성 검증 통과

## 12:47 — 문서 체계 재구성 (결정/작업/보고 분리)

- 루트 `docs\` 신설: `결정 대기.md`(공통 정책 7건), `작업 목록(pioh).md`(사용자 진행 작업 — 키 발급·양식 파일·백업 등), `진행 보고.md`(개발 보고 누적처)
- `백엔드 개발 계획.md`를 front\docs\작업 지시 → **api\docs로 이관** (`2277fea`), `api\docs\결정 대기.md`(백엔드 기술 9건)·`front\docs\결정 대기.md`(연동 시 정리 6건) 신설
- 기존 `작업 지시\미결정 사항.md`는 위 3개 파일로 내용 분배 후 삭제
- 루트 `CLAUDE.md`에 문서 체계 명시 + `@docs/진행 보고.md` 태그 추가 — 이후 개발 보고는 이 파일에 누적
- 파일 원본 보관 질문 답변: 원본은 파기하지 않고 MinIO(자체 호스팅 S3 호환) 보관, DB에는 저장 키+추출 텍스트만. 탈퇴·세션 삭제 시 원본도 정리

### 커밋 문구 제안 (front 저장소)
```
docs: 문서 체계 재구성 — 결정 대기 분리, 백엔드 계획 api로 이관
```

## 14:15 — 결정 대기 결론 전체 반영 + 백엔드 착수

- 스키마 (`363d14a`, 29테이블): industry_benchmark 신설(배치 갱신·표본 수·UNIQUE NULLS NOT DISTINCT), assessment_result.benchmark_snapshot, user.company → **company_id FK 승격**(탈퇴·삭제 시 SET NULL)
- LLM: Anthropic → **OpenAI(GPT) API** (.env.example OPENAI_API_KEY)
- 데모: 고정 계정 불허 결정 반영 — '체험하기' 게스트 자동 계정 방식으로 QnA §5 갱신, DEMO_ACCOUNT_PASSWORD 제거
- 분류 기준 개선: 양식 PDF에 양식번호 없음 → 양식번호 규칙 폐기, 샘플집 분할→LLM 매핑→사람 검수 (reference-forms README)
- 문서: `docs\로직 엔진.md` 작성(사용자 구조: 결정/제안/보류/비교), `docs\ISO9001\워크플로우 표준.md` 작성, api docs는 저장소 추적 제외
- 시드: AX 프로파일링.xlsx 6시트 헤더 구조 파악 — 추출 스크립트 다음 단계

## 14:25 — 시드 추출 완료 (B1) + 문항 수 재확정

- **문항 수 재확정**: xlsx의 CU 문항이 8개(01~07, 09)로 집계 표(7개)와 불일치 → 사용자 확인, **CU-09는 번호 오기 → CU-08로 당겨 총 48문항** 확정. 47 표기 문서 일괄 정정
- **시드 추출**: `scripts/extract-seed.py` → `db/seed/*.json` 10종 (`208b739`, `472c699`) — 48문항·240앵커·12중분류·76소분류·매핑 61건·Lv 5·단계 4·67과제·선행 108건, 검증 assert 전부 통과
- 설문 마스터는 xlsx에 선택지가 없어 제외 — 설문 체계 확정 후 별도 시드
- 필수 업로드 서류 안내 UI 개선 항목을 front 결정 대기 #7로 추가 (커버리지 질문 후속)

### 커밋 문구 제안 (front 저장소)
```
docs: 48문항 재확정 반영 + 결정 대기 갱신
```

## 14:41 — 5축 확정 정리 + 시드 upsert 스크립트

- 5축 48문항 확정 — 구(6축) 체계 기록 제거(로직 엔진·백엔드 개발 계획·결정 대기). 데모 계정 데이터 포함 전환 대상 명시
- 기록 파일 순서 교정: 최신 기록은 맨 하단 누적으로 통일 (본 파일·진행 보고 재정렬)
- 시드 upsert 스크립트 `npm run db:seed` 완성 (`56a31aa`) — 트랜잭션 upsert + 수치 자동 검증, identity BY DEFAULT 전환

## 15:05 — 방법론 C 양식 매핑 + 막힘 해소 가이드

- 샘플 PDF 58p 제목 추출 → 소분류 매핑 완료 (`0331aa4`) — 검수표 `reference-forms\양식 매핑표.md`(medium 5건 확인 필요), 양식 없는 소분류 19종 목록화
- `작업 목록(pioh).md`에 BETTER_AUTH_SECRET 생성법·Contabo 서버 준비 방법(A/B) 가이드 추가

## 15:16 — 양식 매핑 검수 반영

- 문서 기준 재판정으로 medium 5건 중 4건 확정, p30만 확인 대기 (`1115bb2`)
- BETTER_AUTH_SECRET 용도·교체 영향 안내를 작업 목록에 기록

## 15:27 — 양식 매핑 확정 + B3 인증 구현

- p30 자재 입출고증 확정, 매핑 58p 전체 검수 완료 (`1115bb2`…)
- better-auth 인증 구현 (`502d5c3`) — 이메일 인증 필수+Resend, 구글 OAuth, 체험하기 게스트(anonymous), role 입력 차단, CORS. drizzle 업그레이드 동반

## 15:38 — B4 기업 검색·진단 세션 API

- 비즈노 자동완성+국세청 검증, 진단 세션 생성·이력·이어하기 API 구현. requireAuth+zod 검증+CORS

## 16:46 — 로컬 Docker 전체 검증 통과

- migrate(29테이블·트리거14)·seed(48문항 등 전체)·인증 스모크(게스트 로그인→보호 API→401 가드) 전부 통과
- 트리거명 생성 버그 1건 발견·수정

## 16:56 — B5 파일 업로드 구현·실검증

- 업로드 API(MinIO 저장+분류 큐 등록+50개/50MB+화이트리스트) 구현, 로컬 E2E 검증 통과(MinIO 객체·큐 잡 확인). biz_no 부분 유니크 upsert 버그 수정

## 17:20 — B5 분류 워커 구현

- GPT 양식 대조 분류 워커(npm run worker) 구현·E2E 검증. OpenAI 크레딧 부족으로 분류 결과만 미확인(작업 목록에 충전 항목 추가)

## 17:29 — B6 판정·채점 엔진 구현·실검증

- 48문항 판정(자동집계+GPT+결측)·채점(축/종합/Lv/강점병목/균형)·제출·결과 API. E2E: 종합 75→Lv4 등 수치 검증 통과

## 17:47 — 보안 점검·staging 브랜치 + B7 외부 교차검증

- .dockerignore 신설(유출 이력 없음 확인), staging 브랜치 양 저장소 신설
- B7: KIPRIS·DART 교차검증 구현, KIPRIS 실키 호출 검증 통과

## 17:49 — 보안 수정 + B8 과제·로드맵 API

- KIPRIS HTTPS 전환(보안 리뷰 지적). 과제 추천(Lv 핵심과업 기반)·담기·로드맵(선행 폐포·단계 그룹핑) 구현, E2E 수치 검증 통과

## 17:55 — 로드맵 결정 반영 + 설문·HITL·리드 API

- 로드맵 선행 자동 삽입 제거(담은 과제만). 설문 발행(결측 문항 한정)·HITL(admin 403/확정)·리드 API 구현, E2E 통과. Contabo 방법 B 확정 기록

## 18:08 — B9 완성 + GPT 전체 파이프라인 실검증

- 영역 판정·종합 서사 생성 구현. 새 키로 분류(0.95 적중)·판정·영역·서사 전 구간 실검증 통과. L1~L4 판별 기준을 로직 엔진 §1-1로 문서화

## 18:16 — B10 배포 준비물 완성

- Dockerfile(빌드 검증)·운영 compose(내부망 원칙)·운영 마이그레이터·DEPLOY.md 작성. 배포는 사용자 실행만 남음(작업 목록에 절차 기재)

## 18:26 — 프론트 인증 실연동 (1단계)

- lib/api.ts 신설, AuthContext 실제 세션 연동(로그인/가입/게스트/로그아웃/프로필), AuthForm 개편(프리필 제거·동의 체크·인증 메일 안내·체험하기 버튼)
- 백엔드: /api/me + 가입 동의 서버 훅

### 커밋 문구 제안 (front 저장소)
```
feat: 인증 실연동 — 세션 복원·로그인/가입/게스트, 동의 체크·인증 메일 안내
```

## 20:03 — 기록 시각 교정 + 동의 기록 보안 수정

- 본 파일의 시각 헤더 전체를 **실제 커밋 타임스탬프 기준으로 교정** (이전 추정 시각 오류 — 이후 기록은 커밋 시각 기준)
- 보안 리뷰 반영 (`087e096`): 동의 자동 기록을 **이메일 가입 경로로 한정** — 구글 OAuth·게스트는 동의 화면을 거치지 않으므로 기록하지 않음(허위 동의 방지). 소셜 최초 로그인 동의 화면을 front 결정 대기 #8로 등록

## 20:17 — 랜딩 기업 검색·진단 시작 실연동 + 데모 기업 검색 제외

- 백엔드 (`5042994`): company.source에 'demo' 추가 — **데모 기업은 검색 결과에서 제외**(사용자 지시), 데모 시드는 이 출처 사용 예정. 로컬 DB에 제약 반영·제외 동작 검증
- 프론트 랜딩 위저드 실연동:
  - 자동완성: 더미 디렉터리 → **백엔드 검색**(자체 디렉터리+비즈노, 300ms 디바운스)
  - 기업 확인: "맞아요" 클릭 시 **국세청 검증 → 진단 세션 생성**(companyId·assessmentId를 DiagnosisContext에 저장), 실패 사유 표시. 카드 메타는 더미 업종·지역 → 사업자번호 표시로 교체
  - 업로드: 파일 선택 즉시 **서버 업로드**(MinIO+분류 큐), 삭제 시 서버 원본도 삭제
  - 진단 시작하기: 사용 시스템·완료 단계를 서버에 저장 후 이동
  - **데모 버튼(더미 12건 첨부) 제거** — 데모는 '체험하기 게스트+시드' 방식으로 확정되어 구 흐름과 충돌하므로. 복원 원하시면 말씀 필요
- 타입체크 양쪽 통과. 마이그레이션 재생성·정합성 검증 통과

### 커밋 문구 제안 (front 저장소)
```
feat: 랜딩 실연동 — 기업 검색·국세청 검증·세션 생성·실업로드, 데모 버튼 제거
```

## 20:22 — 가입 오류 진단·수정 + 인증 UI 3건

- **Resend 가입 오류 원인 규명**: ① AUTH_EMAIL_FROM 미설정(Missing from) → 코드 폴백(onboarding@resend.dev) 반영(`756a4bd`) ② Resend 계정이 도메인 미검증 **테스트 모드** → 계정 소유자 이메일로만 발송 가능. 도메인 검증 절차를 작업 목록에 기재
- UI: **비밀번호 확인 필드**(불일치 안내), **로그인 오류 한국어 치환**(코드 매핑 — 아이디/비밀번호 오류·미가입·미인증·중복 가입), **Google로 계속하기 버튼**(소셜 로그인 시작). ApiError로 오류 코드 전달 구조 추가
- 로컬 구글 테스트를 위한 리디렉션 URI 등록 항목 작업 목록 추가

### 커밋 문구 제안 (front 저장소)
```
feat: 인증 UI 개선 — 비밀번호 확인·오류 한국어화·구글 로그인 버튼
```

## 20:23 — 배포 가이드 재구성 + 자동 배포 설계

- DEPLOY.md를 **A. 로컬 PC / B. Contabo 서버(최초 1회) / C. 이후 업데이트**로 재구성
- **자동 배포**: main push → GitHub Actions → SSH로 서버 git pull + compose up + 마이그레이션. 운영 .env는 GitHub Secret(PROD_ENV_FILE)로 배포(변경 시 Secret 수정+재실행). 워크플로 파일 작성 완료 — GitHub 저장소 push + Secrets 4개 등록만 하면 활성화

## 20:28 — 배포 워크플로 보안 강화 + 백엔드 저장소 푸시

- 보안 리뷰 4건 수정: 서드파티 SSH 액션 제거(표준 ssh — 공급망 리스크 해소), 호스트 키 검증(DEPLOY_KNOWN_HOSTS), workflow_dispatch main 한정, 시크릿 env 전달
- 푸시 전 최종 점검: .gitignore 확인(env·docs·reference-forms 제외), 추적 58파일 시크릿 스캔 통과(실키 패턴 없음, env.example 플레이스홀더만)
- **axcore-dev/axpoint-analysis-api 원격 연결, develop·staging(=develop 최신화)·main 푸시 완료**
- 이메일 403 재확인: 코드 정상(발신 폴백 적용 확인됨) — Resend 테스트 모드 제약(수신자는 계정 소유자 이메일만). 도메인 검증이 유일한 정식 해결

## 20:36 — 백엔드 main 머지 + collect 화면 실연동

- 백엔드 develop→main 머지·푸시(프리뷰는 front만 필요하다는 결정 반영). 파일 목록 API에 소분류명 포함(`a38d07d`)
- **collect(자료 정리) 실연동**: 시나리오 화면 → 분류 진행 현황(3초 폴링, 파일별 소분류·L레벨·상태) + 제출→판정 완료 폴링→결과 이동. 확정 개편 반영으로 문서확인(HITL 질문)·공개데이터 그리드·워크플로우 비교 섹션은 제외(로직 확정 후 재도입), 수집·판정 로딩 문구는 기존 유지
- 다음: result(진단 결과) 화면 연동

### 커밋 문구 제안 (front 저장소)
```
feat: 자료 정리 실연동 — 분류 현황 폴링·제출·판정 대기, 데모 섹션 제외
```

## 20:41 — result(진단 결과) 화면 실연동

- 결과 API에 축 이름·레벨 이름 조인(`dbe7a66`, develop 푸시)
- result 화면 교체: 서버 판정 데이터 표시 — 기업·Lv 헤더(커버리지 50% 미만 시 "자료 부족" 안내), 5축 점수 바(판정 N/M), 8영역 등급 카드(관리 대상/보통/강점/판단 보류 + As-Is/보류 사유), 종합 서사(결론·강점·보완·전략), "개선 과제 고르러 가기" CTA
- 레이더 차트·업종 평균은 벤치마크 확정 후 재도입, 가치사슬 섹션 삭제(확정), 통계 칩·공개데이터는 로직 확정 대기
- 남은 연동: tasks → roadmap → report → mypage. 이후 로컬 QA → **/security-review 스킬 실행** → front staging 푸시(Vercel 프리뷰)

### 커밋 문구 제안 (front 저장소)
```
feat: 진단 결과 실연동 — 5축 점수·8영역 등급·종합 서사 서버 데이터 표시
```

## 20:47 — dev 오류 해결 + tasks(개선 과제) 화면 실연동

- npm run dev 오류 2건: ① env 플래그 순서(사용자 수정이 정답 — watch 뒤가 아니라 tsx 바로 뒤) ② **MinIO SignatureDoesNotMatch = .env의 MINIO_SECRET_KEY가 로컬 컨테이너 값(axpoint-local)과 불일치** — 값 안내 + MinIO 실패 시 서버 기동은 유지되게 보강(`a0c4e40`)
- tasks 화면 실연동: 67과제 카탈로그 + ★추천(Lv 핵심과업)·기능영역 필터·보유 시스템 선택불가·담기 토글 → 서버 저장 → 로드맵 이동. 기존 문구(과제 N개 담음·2~3개 권장·이 과제로 로드맵 보기 등) 유지

### 커밋 문구 제안 (front 저장소)
```
feat: 개선 과제 실연동 — 67과제·추천·시스템 대체·담기 서버 동기화
```

## 20:55 — env 우선순위 검증 + roadmap 화면 실연동

- env 우선순위 실험 검증: Node 24는 **나중 파일 우선** → 현재 순서(.env → .env.local)에서 .env.local이 이김(관례 일치, 수정 불필요). MinIO 오류의 실원인은 **.env.local의 MINIO_SECRET_KEY 값 자체가 컨테이너 계정(axpoint-local)과 불일치** — 값 수정 또는 컨테이너 재생성 안내
- roadmap 화면 실연동: 서버 재계산 로드맵(단계 그룹핑·기간 누적·자부담 합산) 표시, 선행 자동 삽입 없음(확정). "보고서 보기" CTA 유지
- 확인 과정에서 사용자의 dev 서버(3001)를 종료시킨 실수 — 재실행 안내함

### 커밋 문구 제안 (front 저장소)
```
feat: 로드맵 실연동 — 서버 재계산 단계·기간·자부담 표시
```

## 21:20 — report·mypage 실연동 (프론트 핵심 흐름 연동 완료)

- **report**: 요약 5칸(Lv·종합 점수·담은 과제·총 기간·자부담) 서버 데이터 + "보고서 받기" 모달 → 리드 저장(context=pdf, 진단 연결) → "신청이 접수됐어요. 보고서가 준비되면 {이메일}로 보내드려요"(신규 문구 — 기존 '보냈어요'는 실발송이 없어 부정확). 포지션·ROI 드릴다운은 벤치마크·산출 가정 확정 후, PDF 다운로드는 보고서 구성 확정 후 재도입
- **mypage**: 분석 기록 더미 3건 → 서버 이력(기업명·일시·Lv·완료/진행 중). 재확인 팝업에서 **기록을 진단 컨텍스트로 복원 후 이동**(완료→보고서/결과, 미완료→이어서 진행) — 이어하기 실동작
- 연동 현황: 인증·랜딩·collect·result·tasks·roadmap·report·mypage **전 화면 실연동 완료**. 다음: 로컬 QA → /security-review → staging 푸시(Vercel 프리뷰)

### 커밋 문구 제안 (front 저장소)
```
feat: 보고서·마이페이지 실연동 — 요약·리드 수집·분석 기록·이어하기
```
